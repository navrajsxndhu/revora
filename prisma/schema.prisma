datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Organization {
  id        String      @id @default(uuid())
  name      String
  createdAt DateTime    @default(now())
  workspaces Workspace[]
}

model Workspace {
  id             String      @id @default(uuid())
  name           String
  organizationId String
  organization   Organization @relation(fields: [organizationId], references: [id])
  createdAt      DateTime    @default(now())
  
  members              WorkspaceMember[]
  incidents            Incident[]
  deployments          Deployment[]
  auditLogs            AuditLog[]
  apiTraces            ApiTrace[]
  notificationDeliveries NotificationDelivery[]
  webhookDeliveries    WebhookDelivery[]
  recoveryExecutions   RecoveryExecution[]
  postIncidentReviews  PostIncidentReview[]
  invites              WorkspaceInvite[]
  apiKeys              ApiKey[]
  rateLimitEvents      RateLimitEvent[]
  subscription         Subscription?
  usageRecords         WorkspaceUsage[]
  webhookReplays       WebhookReplay[]
  deadLetters          DeadLetterExecution[]

  benchmarkOptIn       Boolean  @default(true)
  globalReliabilityTier String?

  serviceGovernance    ServiceGovernance[]
  governanceEvents     GovernanceEvent[]

  regionalHealth       RegionalServiceHealth[]
  simulations          SimulationExecution[]

  regionalCoordinators RegionalCoordinator[]
  federatedEvents      FederatedEvent[]
  digitalTwinStates    DigitalTwinState[]
  chaosSimulations     ChaosSimulation[]
  rollbackExecutions   RollbackExecution[]

  infrastructurePulses   InfrastructurePulse[]
  migrationExecutions    MigrationExecution[]
  topologyRebalances     TopologyRebalance[]
  regionalStressForecasts RegionalStressForecast[]

  governanceLearningEvents GovernanceLearningEvent[]
  reliabilityDNAs          ReliabilityDNA[]
  antiPatternEvents        AntiPatternEvent[]
  
  reliabilityLedgerEvents ReliabilityLedgerEvent[]
  reliabilityBudgets      ReliabilityBudget[]
  operationalDebtProfiles OperationalDebtProfile[]

  reliabilityCapitalAllocations ReliabilityCapitalAllocation[]
  organizationalPressureZones   OrganizationalPressureZone[]
  reliabilityTreasurySnapshots  ReliabilityTreasurySnapshot[]
  deploymentCongestionEvents    DeploymentCongestionEvent[]

  reliabilityConstitutions      ReliabilityConstitution[]
  constitutionalViolations      ConstitutionalViolation[]

  governanceEntropySnapshots    GovernanceEntropySnapshot[]
  institutionalDoctrines        InstitutionalDoctrine[]
  organizationalLineages        OrganizationalLineage[]
  operationalContinuitySnapshots OperationalContinuitySnapshot[]

  reliabilityTreaties            ReliabilityTreaty[]
  ecosystemDependencies          EcosystemDependency[]

  infrastructureSurvivabilitySnapshots InfrastructureSurvivabilitySnapshot[]
  energyGovernanceProfiles             EnergyGovernanceProfile[]
  climateExposureZones                 ClimateExposureZone[]
  hardwareLifecycleRecords             HardwareLifecycleRecord[]

  temporalDriftSnapshots               TemporalDriftSnapshot[]
  temporalDebtProfiles                 TemporalDebtProfile[]
  civilizationEvolutionRecords         CivilizationEvolutionRecord[]
  futureContinuityProjections          FutureContinuityProjection[]

  systemicAwarenessSnapshots           SystemicAwarenessSnapshot[]
  operationalPerceptionRecords         OperationalPerceptionRecord[]
  consciousnessProjections             ConsciousnessProjection[]

  operationalPathogenEvents            OperationalPathogenEvent[]
  governanceIntegritySnapshots         GovernanceIntegritySnapshot[]
  continuityAntibodyRecords            ContinuityAntibodyRecord[]

  adaptationEvidences                  AdaptationEvidence[]
  governanceOptimizations              GovernanceOptimization[]
  convergenceSnapshots                 ConvergenceSnapshot[]
  adaptationSimulations                AdaptationSimulation[]

  operationalKnowledge                 OperationalKnowledge[]
  operationalDoctrines                 OperationalDoctrine[]
  operationalWisdom                    OperationalWisdom[]
  institutionalMemoryLinks             InstitutionalMemoryLink[]

  operationalDecisions                 OperationalDecision[]
  decisionSimulations                  DecisionSimulation[]

  operationalExecutions                OperationalExecution[]
  operationalIntents                   OperationalIntent[]
  operationalDrifts                    OperationalDriftRecord[]

  operationalOptimizations             OperationalOptimization[]
  optimizationSimulations              OptimizationSimulation[]

  operationalPlans                     OperationalPlan[]
  planSimulations                      PlanSimulation[]

  operationalCoordinations             OperationalCoordination[]
  operationalIntelligenceSnapshots     OperationalIntelligenceSnapshot[]
  intelligenceSimulations              IntelligenceSimulation[]

  integrationConnectors                IntegrationConnector[]
  integrationEvents                    IntegrationEvent[]
  integrationSynchronizations          IntegrationSynchronization[]
  integrationHealthSnapshots           IntegrationHealthSnapshot[]

  enterpriseReleases                   EnterpriseRelease[]

  releaseVersions            ReleaseVersion[]
  releaseArtifacts           ReleaseArtifact[]
  deploymentPlans            DeploymentPlan[]
  deploymentExecutions       DeploymentExecution[]
  deploymentEnvironments     DeploymentEnvironment[]
  deploymentWindows          DeploymentWindow[]
  deploymentApprovals        DeploymentApproval[]
  deploymentValidations      DeploymentValidation[]
  progressiveRollouts        ProgressiveRollout[]
  rolloutStages              RolloutStage[]
  deploymentCheckpoints      DeploymentCheckpoint[]
  productionVerifications    ProductionVerification[]
  releaseAssessments         ReleaseAssessment[]
  releaseSnapshots           ReleaseSnapshot[]
  releaseTimelines           ReleaseTimeline[]
  releaseLedgers             ReleaseLedger[]
  releaseIndices             ReleaseIndex[]
  deploymentHealths          DeploymentHealth[]
  deploymentTargets DeploymentTarget[]
  enterpriseApis                 EnterpriseApi[]
  apiConsumers                   ApiConsumer[]
  apiProviders                   ApiProvider[]
  changeApprovals                ChangeApproval[]
  changeCalendarEvents           ChangeCalendarEvent[]
  changeImpactAssessments        ChangeImpactAssessment[]
  operationalChanges             OperationalChange[]
  enterpriseEvents               EnterpriseEvent[]
  governanceAssessments          GovernanceAssessment[]
  policyExceptions               PolicyException[]
  governancePolicies             GovernancePolicy[]
  riskRegisters                  RiskRegister[]
  operationalIncidents           OperationalIncident[]
  strategicInitiatives           StrategicInitiative[]
  operationalInsights            OperationalInsight[]
  knowledgeCoverages             KnowledgeCoverage[]
  telemetryRetentions            TelemetryRetention[]
  serviceCatalogs                ServiceCatalog[]
  resourcePolicies               ResourcePolicy[]
  provisioningRequests           ProvisioningRequest[]
  platformResources              PlatformResource[]
  portfolioObjectives            PortfolioObjective[]
  errorBudgetLedgers             ErrorBudgetLedger[]
  reliabilityForecasts           ReliabilityForecast[]
  serviceLevelIndicators         ServiceLevelIndicator[]
  serviceLevelObjectives         ServiceLevelObjective[]
  serviceApprovals               ServiceApproval[]
  serviceRequests                ServiceRequest[]
  serviceKnowledges              ServiceKnowledge[]
  serviceQueues                  ServiceQueue[]
  businessCapabilities           BusinessCapability[]
  microservices                  Microservice[]
  topologyNodes                  TopologyNode[]
  topologyEdges                  TopologyEdge[]
  operationalWorkflows           OperationalWorkflow[]
  apiContracts                   ApiContract[]
  apiIndexes                     ApiIndex[]
  apiLedgers                     ApiLedger[]
  apiEndpoints                   ApiEndpoint[]
  integrationFlows               IntegrationFlow[]
  eventChannels                  EventChannel[]
  messageContracts               MessageContract[]
  apiPolicies                    ApiPolicy[]
  schemaRegistries               SchemaRegistry[]
  apiSubscriptions               ApiSubscription[]
  webhookEndpoints               WebhookEndpoint[]
  configurationItems             ConfigurationItem[]
  assetRelationships             AssetRelationship[]
  configurationSnapshots         ConfigurationSnapshot[]
  serviceTopologies              ServiceTopology[]
  dataClassifications            DataClassification[]
  enterpriseDatasets             EnterpriseDataset[]
  dataRetentionPolicies          DataRetentionPolicy[]
  dataLifecycles                 DataLifecycle[]
  dataLineages                   DataLineage[]
  masterDataEntities             MasterDataEntity[]
  metadataCatalogs               MetadataCatalog[]
  metadataIndexes                MetadataIndex[]
  metadataLedgers                MetadataLedger[]
  dataOwners                     DataOwner[]
  dataQualityRules               DataQualityRule[]
  referenceDataEntities          ReferenceDataEntity[]
  simulationCheckpoints          SimulationCheckpoint[]
  twinHealths                    TwinHealth[]
  digitalTwinLedgers             DigitalTwinLedger[]
  twinSimulationExecutions       TwinSimulationExecution[]
  simulationScenarios            SimulationScenario[]
  simulationEvidences            SimulationEvidence[]
  failureScenarios               FailureScenario[]
  impactAssessments              ImpactAssessment[]
  phase134RecoveryScenarios      Phase134RecoveryScenario[]
  replaySessions                 ReplaySession[]
  stateTransitions               StateTransition[]
  twinStates                     TwinState[]
  validationResults              ValidationResult[]
  eventConsumers                 EventConsumer[]
  deliveryPolicies               DeliveryPolicy[]
  eventTopics                    EventTopic[]
  eventQueues                    EventQueue[]
  eventStreams                   EventStream[]
  eventProducers                 EventProducer[]
  retryPolicies                  RetryPolicy[]
  routingPolicies                RoutingPolicy[]
  streamingIndexes               StreamingIndex[]
  streamingLedgers               StreamingLedger[]
  eventSubscriptions             EventSubscription[]
  budgetPolicies                 BudgetPolicy[]
  cloudCostRecords               CloudCostRecord[]
  finOpsSnapshots                FinOpsSnapshot[]
  resourceOptimizations          ResourceOptimization[]
  accessPolicies                 AccessPolicy[]
  accessReviews                  AccessReview[]
  accessRequests                 AccessRequest[]
  approvalWorkflows              ApprovalWorkflow[]
  apiCredentials                 ApiCredential[]
  emergencyAccesses              EmergencyAccess[]
  enterpriseIdentities           EnterpriseIdentity[]
  enterpriseRoles                EnterpriseRole[]
  privilegedSessions             PrivilegedSession[]
  identityLedgers                IdentityLedger[]
  identityProviders              IdentityProvider[]
  identityRisks                  IdentityRisk[]
  permissions                    Permission[]
  serviceAccounts                ServiceAccount[]
  separationOfDutyRules          SeparationOfDutyRule[]
  incidentTimelineEvents         IncidentTimelineEvent[]
  incidentRecoveryPlans          IncidentRecoveryPlan[]
  rootCauseEvidences             RootCauseEvidence[]
  businessImpacts                BusinessImpact[]
  businessObjectives             BusinessObjective[]
  operationalCorrelations        OperationalCorrelation[]
  decisionPackages               DecisionPackage[]
  decisionLedgers                DecisionLedger[]
  evidenceCorrelations           EvidenceCorrelation[]
  executiveBriefings             ExecutiveBriefing[]
  executiveKPIs                  ExecutiveKPI[]
  enterpriseHealths              EnterpriseHealth[]
  enterpriseMetrics              EnterpriseMetric[]
  departmentScorecards           DepartmentScorecard[]
  strategicRisks                 StrategicRisk[]
  operationalTrends              OperationalTrend[]
  architectureDecisions          ArchitectureDecision[]
  decisionMemories               DecisionMemory[]
  knowledgeReviews               KnowledgeReview[]
  knowledgeGaps                  KnowledgeGap[]
  knowledgeArticles              KnowledgeArticle[]
  knowledgeDomains               KnowledgeDomain[]
  knowledgeCategories            KnowledgeCategory[]
  knowledgeEvidences             KnowledgeEvidence[]
  knowledgeGraphNodes            KnowledgeGraphNode[]
  knowledgeGraphEdges            KnowledgeGraphEdge[]
  knowledgeIndexes               KnowledgeIndex[]
  knowledgeLedgers               KnowledgeLedger[]
  playbookKnowledges             PlaybookKnowledge[]
  knowledgeRelationships         KnowledgeRelationship[]
  runbookKnowledges              RunbookKnowledge[]
  telemetryAlerts                TelemetryAlert[]
  telemetryCorrelations          TelemetryCorrelation[]
  diagnosticSessions             DiagnosticSession[]
  telemetryMetrics               TelemetryMetric[]
  telemetryLogs                  TelemetryLog[]
  telemetryTraces                TelemetryTrace[]
  healthIndicators               HealthIndicator[]
  observabilityAssessments       ObservabilityAssessment[]
  observabilitySnapshots         ObservabilitySnapshot[]
  telemetrySources               TelemetrySource[]
  telemetryIndexes               TelemetryIndex[]
  telemetryLedgers               TelemetryLedger[]
  executionApprovals             ExecutionApproval[]
  executionCheckpoints           ExecutionCheckpoint[]
  executionConstraints           ExecutionConstraint[]
  taskDependencies               TaskDependency[]
  executionPlans                 ExecutionPlan[]
  pipelineExecutions             PipelineExecution[]
  executionTasks                 ExecutionTask[]
  executionMetricses             ExecutionMetrics[]
  executionLedgers               ExecutionLedger[]
  executionLocks                 ExecutionLock[]
  executionPipelines             ExecutionPipeline[]
  executionQueues                ExecutionQueue[]
  aopExecutionReplaies           AopExecutionReplay[]
  executionReservations          ExecutionReservation[]
  executionValidations           ExecutionValidation[]
  orchestrationWorkflows         OrchestrationWorkflow[]
  capacityAllocations            CapacityAllocation[]
  planningAssessments            PlanningAssessment[]
  enterpriseCapacities           EnterpriseCapacity[]
  capacityConstraints            CapacityConstraint[]
  demandRequests                 DemandRequest[]
  planningIndexes                PlanningIndex[]
  planningEvidences              PlanningEvidence[]
  planningLedgers                PlanningLedger[]
  capacityReservations           CapacityReservation[]
  capacityResources              CapacityResource[]
  planningScenarios              PlanningScenario[]
  capacityUtilizations           CapacityUtilization[]
  portfolioAssessments           PortfolioAssessment[]
  benefitRealizations            BenefitRealization[]
  initiativeDependencies         InitiativeDependency[]
  portfolioIndexes               PortfolioIndex[]
  transformationPrograms         TransformationProgram[]
  portfolioInitiatives           PortfolioInitiative[]
  investmentAllocations          InvestmentAllocation[]
  enterprisePortfolios           EnterprisePortfolio[]
  portfolioEvidences             PortfolioEvidence[]
  portfolioLedgers               PortfolioLedger[]
  executiveRoadmaps              ExecutiveRoadmap[]
  valueStreams                   ValueStream[]
  releasePackages                ReleasePackage[]
  releaseRiskProfiles            ReleaseRiskProfile[]
  releaseStrategies              ReleaseStrategy[]
  releaseWindows                 ReleaseWindow[]
  businessContinuityPlans        BusinessContinuityPlan[]
  businessImpactAnalysises       BusinessImpactAnalysis[]
  recoveryCheckpoints            RecoveryCheckpoint[]
  continuityLedgers              ContinuityLedger[]
  recoveryDependencies           RecoveryDependency[]
  recoveryPlans                  RecoveryPlan[]
  resilienceIndexes              ResilienceIndex[]
  recoveryReadinesses            RecoveryReadiness[]
  recoveryExercises              RecoveryExercise[]
  failbackPlans                  FailbackPlan[]
  failoverExecutions             FailoverExecution[]
  phase134RecoveryExecutions     Phase134RecoveryExecution[]
  recoveryValidations            RecoveryValidation[]
  resilienceAssessments          ResilienceAssessment[]
  complianceControls             ComplianceControl[]
  securityAuditSnapshots         SecurityAuditSnapshot[]
  securityFindings               SecurityFinding[]
  securityPolicies               SecurityPolicy[]
  applicationPortfolios          ApplicationPortfolio[]
  enterpriseAssets               EnterpriseAsset[]
  blastRadiusAnalysises          BlastRadiusAnalysis[]
  cloudAccounts                  CloudAccount[]
  serviceDependencies            ServiceDependency[]
  businessServices               BusinessService[]
  infrastructureResources        InfrastructureResource[]
  operationalLineages            OperationalLineage[]
  networkZones                   NetworkZone[]
  topologyLedgers                TopologyLedger[]
  operationalWorkflowExecutions  OperationalWorkflowExecution[]
}

model WorkspaceMember {
  id          String    @id @default(uuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  userId      String
  user        User      @relation(fields: [userId], references: [id])
  role        String    // OWNER, OPERATOR, VIEWER
  createdAt   DateTime  @default(now())
  
  warRoomParticipations WarRoomParticipant[]

  @@unique([workspaceId, userId])
}

model User {
  id        String   @id @default(uuid())
  email     String   @unique
  password  String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  memberships WorkspaceMember[]
  sessions    AuthSession[]
}

model AuthSession {
  id           String   @id @default(uuid())
  userId       String
  user         User     @relation(fields: [userId], references: [id])
  tokenHash    String   @unique
  fingerprint  String?
  expiresAt    DateTime
  createdAt    DateTime @default(now())
  lastActiveAt DateTime @default(now())
}

model ApiTrace {
  id                  String   @id @default(uuid())
  workspaceId         String
  workspace           Workspace @relation(fields: [workspaceId], references: [id])
  workflowExecutionId String
  nodeId              String
  status              String
  latencyMs           Int
  requestPayload      String?
  responsePayload     String?
  errorMessage        String?
  timestamp           DateTime @default(now())
}

model Incident {
  id                     String         @id @default(uuid())
  workspaceId            String
  workspace              Workspace      @relation(fields: [workspaceId], references: [id])
  title                  String
  description            String
  state                  String         // OPEN, ACKNOWLEDGED, INVESTIGATING, MITIGATING, RECOVERING, RESOLVED
  severity               String         @default("MEDIUM") // LOW, MEDIUM, HIGH, CRITICAL
  recurringCount         Int            @default(0)
  relatedTraceId         String
  correlationId          String?        
  deploymentId           String?
  deployment             Deployment?    @relation(fields: [deploymentId], references: [id])
  serviceAffected        String?
  rootCauseSummary       String?
  recommendedAction      String?
  probableCommit         String?
  deploymentDiff         String?        // JSON summary of diffs
  
  // Current Snapshot Fields
  assignedTo             String?        // Operator assigned to incident
  incidentCommander      String?        // Operational coordination owner
  team                   String?        // Optional organizational mapping
  resolvedSuccessfully   Boolean        @default(true)
  rollbackTriggered      Boolean        @default(false)
  slackThreadTs          String?        
  activeRunbookId        String?
  completedRunbookSteps  String?        @default("[]") // JSON string array of step IDs
  
  createdAt              DateTime       @default(now())
  updatedAt              DateTime       @updatedAt
  
  events                 IncidentEvent[]
  notes                  IncidentNote[]
  postIncidentReview     PostIncidentReview?
  warRoom                WarRoom?

  
  masterIncidentId       String?
  masterIncident         Incident?      @relation("IncidentToIncident", fields: [masterIncidentId], references: [id])
  childIncidents         Incident[]     @relation("IncidentToIncident")
  groupedCount           Int            @default(0)

  isSymptom              Boolean        @default(false)
  rootCauseIncidentId    String?
  probableRootService    String?
  rootCauseIncident      Incident?      @relation("RootCauseToIncident", fields: [rootCauseIncidentId], references: [id])
  symptomIncidents       Incident[]     @relation("RootCauseToIncident")

  recoveryStatus         String?        // PENDING | IN_PROGRESS | COMPLETED
  recoveryStartedAt      DateTime?
  recoveryCompletedAt    DateTime?
  
  recoveryConfidence     Int?
  historicalRecoveryCount Int?
  lastSuccessfulRemediation DateTime?
  
  failureSignature       String?
  probableFailurePoint   String?
  deploymentRiskLevel    String?
  historicalSimilarityCount Int? @default(0)
  deadLetters            DeadLetterExecution[]

  aiSummary              String?
  aiSummaryGeneratedAt   DateTime?
  summaryVersion         Int      @default(1)

  isEligibleForBenchmarking Boolean @default(true)
}

model IncidentEvent {
  id          String   @id @default(uuid())
  incidentId  String
  incident    Incident @relation(fields: [incidentId], references: [id])
  eventType   String   // ASSIGNMENT, ESCALATION, RESOLUTION, REMEDIATION, RECOVERY, COMMANDER_CHANGE, STATE_TRANSITION
  payload     String?  // JSON context
  actor       String?  // User or system
  createdAt   DateTime @default(now())
}

model IncidentNote {
  id         String   @id @default(uuid())
  incidentId String
  incident   Incident @relation(fields: [incidentId], references: [id])
  authorId   String
  message    String
  createdAt  DateTime @default(now())
}

model WorkflowExecution {
  id             String    @id @default(uuid())
  workflowId     String
  state          String    // QUEUED, PROCESSING, COMPLETED, FAILED, RETRYING
  autoReplayed   Boolean   @default(false)
  startedAt      DateTime  @default(now())
  completedAt    DateTime?
  retryCount     Int       @default(0)
  failureReason  String?
}

model AuditLog {
  id            String   @id @default(uuid())
  workspaceId   String
  workspace     Workspace @relation(fields: [workspaceId], references: [id])
  executionId   String
  eventType     String
  status        String
  message       String?
  actor         String?        // Operator or System identity
  createdAt     DateTime       @default(now())
}

model Deployment {
  id                String     @id @default(uuid())
  workspaceId       String
  workspace         Workspace  @relation(fields: [workspaceId], references: [id])
  serviceName       String
  commitSha         String
  environment       String
  changedFiles      String     // JSON string of files changed
  dependencyChanges String?    // JSON string of dependencies changed
  team              String?
  riskLevel         String     // LOW, MEDIUM, HIGH, CRITICAL
  riskReasoning     String
  rolloutStrategy   String
  status            String     @default("EVALUATED") // EVALUATED, SHIPPED, ROLLED_BACK
  rollbackCount     Int        @default(0)
  createdAt         DateTime   @default(now())
  incidents         Incident[]
  isEligibleForBenchmarking Boolean @default(true)
}

model NotificationDelivery {
  id            String   @id @default(uuid())
  workspaceId   String
  workspace     Workspace @relation(fields: [workspaceId], references: [id])
  provider      String   // e.g., SLACK
  status        String   // PENDING, DELIVERED, FAILED
  retries       Int      @default(0)
  responseCode  Int?
  deliveredAt   DateTime?
  payload       String?
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
}

model WebhookDelivery {
  id            String   @id @default(uuid())
  workspaceId   String
  workspace     Workspace @relation(fields: [workspaceId], references: [id])
  deliveryId    String   @unique
  provider      String   // GITHUB, VERCEL
  payloadHash   String
  processedAt   DateTime?
  status        String   // PENDING, PROCESSED, FAILED, DUPLICATE
  createdAt     DateTime @default(now())
}

model RecoveryExecution {
  id            String   @id @default(uuid())
  workspaceId   String
  workspace     Workspace @relation(fields: [workspaceId], references: [id])
  incidentId    String
  currentStep   Int      @default(0)
  totalSteps    Int
  state         String   // QUEUED, ACTIVE, COMPLETED, FAILED, STALLED
  retries       Int      @default(0)
  startedAt     DateTime?
  completedAt   DateTime?
  commitsIncluded Int      @default(0)

  isEligibleForBenchmarking Boolean @default(true)

  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
}

model PostIncidentReview {
  id                  String   @id @default(uuid())
  workspaceId         String
  workspace           Workspace @relation(fields: [workspaceId], references: [id])
  incidentId          String    @unique
  incident            Incident  @relation(fields: [incidentId], references: [id])
  title               String
  summary             String
  rootCause           String
  impact              String
  timelineMarkdown    String
  resolutionMarkdown  String
  preventativeActions String
  status              String    // DRAFT, REVIEWED, APPROVED
  generatedAt         DateTime  @default(now())
  approvedAt          DateTime?
  approvedBy          String?

  aiSummary              String?
  aiSummaryGeneratedAt   DateTime?
  summaryVersion         Int      @default(1)
}

model WorkspaceInvite {
  id          String    @id @default(uuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  email       String
  role        String    // OWNER, OPERATOR, VIEWER
  token       String    @unique
  expiresAt   DateTime
  invitedBy   String
  acceptedAt  DateTime?
  createdAt   DateTime  @default(now())
}

model ApiKey {
  id          String    @id @default(uuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  name        String
  keyHash     String
  prefix      String    // e.g., "rva_" for easy identification
  scopes      String    // JSON array of scopes like ["incident:read", "deployment:write"]
  lastUsed    DateTime?
  revoked     Boolean   @default(false)
  createdAt   DateTime  @default(now())
}

model RateLimitEvent {
  id          String    @id @default(uuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  route       String
  identifier  String    // IP or API Key prefix
  exceededAt  DateTime  @default(now())
}

model Subscription {
  id                    String   @id @default(uuid())
  workspaceId           String   @unique
  workspace             Workspace @relation(fields: [workspaceId], references: [id])
  stripeCustomerId      String   @unique
  stripeSubscriptionId  String?  @unique
  stripePriceId         String?
  status                String   // TRIALING, ACTIVE, PAST_DUE, CANCELED, INCOMPLETE
  currentPeriodEnd      DateTime?
  cancelAtPeriodEnd     Boolean  @default(false)
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt
}

model WorkspaceUsage {
  id              String   @id @default(uuid())
  workspaceId     String
  workspace       Workspace @relation(fields: [workspaceId], references: [id])
  billingPeriodId String   // e.g. "2023-10"
  metric          String   // INCIDENT_COUNT, RECOVERY_EXECUTIONS, API_REQUESTS
  value           Int      @default(0)
  updatedAt       DateTime @updatedAt
  
  @@unique([workspaceId, billingPeriodId, metric])
}

model WebhookReplay {
  id          String   @id @default(uuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  provider    String   // 'github', 'vercel', etc.
  payload     String   // The raw JSON payload stringified
  headers     String   // The headers stringified
  status      String   // 'FAILED', 'REPLAYED'
  errorMessage String?
  createdAt   DateTime @default(now())
  replayedAt  DateTime?
}

model DeadLetterExecution {
  id               String   @id @default(uuid())
  workspaceId      String
  workspace        Workspace @relation(fields: [workspaceId], references: [id])
  incidentId       String
  incident         Incident @relation(fields: [incidentId], references: [id])
  recoveryActionId String
  payload          String   // The state when it failed
  errorReason      String
  retryCount       Int      @default(0)
  status           String   // 'PENDING_REVIEW', 'REPLAYED', 'DISCARDED'
  createdAt        DateTime @default(now())
  resolvedAt       DateTime?
}




model ServiceGovernance {
  id                      String    @id @default(uuid())
  workspaceId             String
  workspace               Workspace @relation(fields: [workspaceId], references: [id])
  serviceName             String
  reliabilityBudget       Int       @default(100)
  deploymentFreezeActive  Boolean   @default(false)
  freezeReason            String?
  rolloutRestrictionLevel String    @default("SAFE") // SAFE, WATCHLIST, RESTRICTED, FROZEN
  createdAt               DateTime  @default(now())
  updatedAt               DateTime  @updatedAt

  @@unique([workspaceId, serviceName])
}

model GovernanceEvent {
  id          String   @id @default(uuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  serviceName String
  actionType  String   // FREEZE_APPLIED, FREEZE_LIFTED, BUDGET_DECREASED, RESTRICTION_ESCALATED
  reasoning   String
  actor       String   // e.g. SYSTEM, OPERATOR_NAME
  createdAt   DateTime @default(now())
}

model WarRoom {
  id          String   @id @default(uuid())
  incidentId  String   @unique
  incident    Incident @relation(fields: [incidentId], references: [id])
  status      String   @default("ACTIVE") // ACTIVE, ARCHIVED
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  participants WarRoomParticipant[]
}

model WarRoomParticipant {
  id          String   @id @default(uuid())
  warRoomId   String
  warRoom     WarRoom  @relation(fields: [warRoomId], references: [id])
  memberId    String
  member      WorkspaceMember @relation(fields: [memberId], references: [id])
  joinedAt    DateTime @default(now())
  leftAt      DateTime?
}

model RegionalServiceHealth {
  id          String   @id @default(uuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  region      String
  serviceName String
  healthState String   @default("HEALTHY") // HEALTHY, DEGRADED, FAILING
  lastUpdated DateTime @default(now())

  @@unique([workspaceId, region, serviceName])
}

model SimulationExecution {
  id              String   @id @default(uuid())
  workspaceId     String
  workspace       Workspace @relation(fields: [workspaceId], references: [id])
  serviceName     String
  simulatedRisk   Int
  recommendation  String
  createdAt       DateTime @default(now())
}

model RegionalCoordinator {
  id          String   @id @default(uuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  region      String
  endpoint    String
  status      String   @default("ACTIVE")
  lastPing    DateTime @default(now())

  @@unique([workspaceId, region])
}

model FederatedEvent {
  id          String   @id @default(uuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  originRegion String
  eventType   String
  payload     String
  signature   String
  timestamp   DateTime @default(now())
}

model DigitalTwinState {
  id          String   @id @default(uuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  snapshot    String   // JSON payload of topology and state
  timestamp   DateTime @default(now())
}

model ChaosSimulation {
  id          String   @id @default(uuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  scenario    String
  targetRegion String?
  targetService String?
  status      String   @default("RUNNING") // RUNNING, COMPLETED, FAILED
  result      String?
  createdAt   DateTime @default(now())
}

model RollbackExecution {
  id          String   @id @default(uuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  deploymentId String
  serviceName  String
  targetRegions String // JSON array of regions
  status      String   @default("PENDING") // PENDING, EXECUTING, COMPLETED, FAILED
  createdAt   DateTime @default(now())
}

model InfrastructurePulse {
  id          String   @id @default(uuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  region      String
  latencyMs   Int
  activeQueues Int
  federationLagMs Int
  timestamp   DateTime @default(now())
}

model MigrationExecution {
  id          String   @id @default(uuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  serviceName String
  sourceRegion String
  targetRegion String
  reason      String
  status      String   @default("RECOMMENDED") // RECOMMENDED, EXECUTING, COMPLETED
  createdAt   DateTime @default(now())
}

model TopologyRebalance {
  id          String   @id @default(uuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  serviceName String
  action      String   // THROTTLE, DECOUPLE, REROUTE
  impact      String
  status      String   @default("ACTIVE")
  createdAt   DateTime @default(now())
}

model RegionalStressForecast {
  id          String   @id @default(uuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  region      String
  stressScore Float
  predictedSaturation DateTime?
  createdAt   DateTime @default(now())
}

model GovernanceLearningEvent {
  id                String   @id @default(cuid())
  workspaceId       String
  workspace         Workspace @relation(fields: [workspaceId], references: [id])
  service           String
  policyType        String
  previousValue     Float
  newValue          Float
  reason            String
  confidenceScore   Float
  createdAt         DateTime @default(now())
}

model ReliabilityDNA {
  id                         String   @id @default(cuid())
  workspaceId                String
  workspace                  Workspace @relation(fields: [workspaceId], references: [id])
  service                    String
  mttrTrend                  Float
  rollbackAcceleration       Float
  blastRadiusGrowth          Float
  governanceViolationRate    Float
  recoveryReliability        Float
  stressSensitivity          Float
  reliabilityTier            String
  updatedAt                  DateTime @updatedAt
  
  @@unique([workspaceId, service])
}

model AntiPatternEvent {
  id                String   @id @default(cuid())
  workspaceId       String
  workspace         Workspace @relation(fields: [workspaceId], references: [id])
  service           String
  patternType       String
  severity          String
  evidence          String   // JSON stringified
  recommendation    String
  createdAt         DateTime @default(now())
}

model ReliabilityLedgerEvent {
  id                String   @id @default(cuid())
  workspaceId       String
  workspace         Workspace @relation(fields: [workspaceId], references: [id])
  service           String
  team              String?
  eventType         String   // DEPLOYMENT_SPEND, INCIDENT_DEBT, ROLLBACK_PENALTY, STABILITY_REWARD, GOVERNANCE_TAX, VELOCITY_BONUS
  reliabilityCost   Int      @default(0)
  operationalCredit Int      @default(0)
  debtImpact        Int      @default(0)
  governanceTax     Int      @default(0)
  createdAt         DateTime @default(now())
}

model ReliabilityBudget {
  id                String   @id @default(cuid())
  workspaceId       String
  workspace         Workspace @relation(fields: [workspaceId], references: [id])
  service           String
  currentBudget     Int      @default(1000)
  burnRate          Float    @default(0.0)
  exhaustionRisk    String   @default("SUSTAINABLE") // SUSTAINABLE, AGGRESSIVE, UNSAFE, CRITICAL
  stabilityScore    Float    @default(100.0)
  deploymentQuota   Int      @default(10)
  lastRecalculatedAt DateTime @default(now())

  @@unique([workspaceId, service])
}

model OperationalDebtProfile {
  id                     String   @id @default(cuid())
  workspaceId            String
  workspace              Workspace @relation(fields: [workspaceId], references: [id])
  service                String
  debtScore              Float    @default(0.0)
  recurringIncidentRate  Float    @default(0.0)
  governancePenaltyScore Float    @default(0.0)
  recoveryEfficiency     Float    @default(100.0)
  debtDecayRate          Float    @default(1.0)
  updatedAt              DateTime @updatedAt

  @@unique([workspaceId, service])
}

model ReliabilityCapitalAllocation {
  id                 String   @id @default(cuid())
  workspaceId        String
  workspace          Workspace @relation(fields: [workspaceId], references: [id])
  service            String
  allocatedCapital   Int      @default(0)
  deploymentWeight   Float    @default(1.0)
  governancePriority String   @default("NORMAL")
  exhaustionRisk     String   @default("SUSTAINABLE")
  createdAt          DateTime @default(now())

  @@unique([workspaceId, service])
}

model OrganizationalPressureZone {
  id                   String   @id @default(cuid())
  workspaceId          String
  workspace            Workspace @relation(fields: [workspaceId], references: [id])
  region               String
  pressureScore        Float    @default(0.0)
  instabilityDensity   Float    @default(0.0)
  deploymentCongestion String   @default("STABLE") // STABLE, COMPRESSED, CONGESTED, SATURATED
  debtConcentration    Float    @default(0.0)
  updatedAt            DateTime @updatedAt

  @@unique([workspaceId, region])
}

model ReliabilityTreasurySnapshot {
  id                     String   @id @default(cuid())
  workspaceId            String
  workspace              Workspace @relation(fields: [workspaceId], references: [id])
  availableCapital       Int      @default(0)
  reservedCapital        Int      @default(0)
  debtExposure           Int      @default(0)
  organizationalBurnRate Float    @default(0.0)
  deploymentLiquidity    Float    @default(0.0)
  capturedAt             DateTime @default(now())
}

model DeploymentCongestionEvent {
  id                 String   @id @default(cuid())
  workspaceId        String
  workspace          Workspace @relation(fields: [workspaceId], references: [id])
  congestionLevel    String   // STABLE, COMPRESSED, CONGESTED, SATURATED
  affectedServices   String   // JSON string of services
  rolloutCompression Float    @default(0.0)
  mitigationStrategy String
  createdAt          DateTime @default(now())
}

model ReliabilityConstitution {
  id                           String   @id @default(cuid())
  workspaceId                  String   @unique
  workspace                    Workspace @relation(fields: [workspaceId], references: [id])
  constitutionVersion          Int      @default(1)
  activeAmendmentCount         Int      @default(0)
  constitutionalStabilityScore Float    @default(100.0)
  treasuryProtectionLevel      String   @default("STRICT")
  createdAt                    DateTime @default(now())

  policies                     ConstitutionalPolicy[]
  amendments                   ConstitutionalAmendment[]
}

model ConstitutionalPolicy {
  id               String   @id @default(cuid())
  constitutionId   String
  constitution     ReliabilityConstitution @relation(fields: [constitutionId], references: [id])
  policyType       String   // DEPLOYMENT_LAW, FREEZE_LAW, DEBT_CEILING, etc.
  policyDefinition String   // JSON
  enforcementLevel String   // BLOCKING, AUDIT_ONLY
  immutableHash    String   @unique
  createdAt        DateTime @default(now())
}

model ConstitutionalViolation {
  id                 String   @id @default(cuid())
  workspaceId        String
  workspace          Workspace @relation(fields: [workspaceId], references: [id])
  service            String
  region             String?
  violationType      String
  severity           String   // MINOR, MAJOR, SEVERE, CRITICAL_BREACH
  operationalImpact  String
  governanceContext  String
  detectedAt         DateTime @default(now())
}

model ConstitutionalAmendment {
  id                String   @id @default(cuid())
  constitutionId    String
  constitution      ReliabilityConstitution @relation(fields: [constitutionId], references: [id])
  amendmentTitle    String
  proposedChange    String   // JSON
  simulationOutcome String?
  approvalStatus    String   @default("PENDING_SIMULATION") // PENDING_SIMULATION, REJECTED, APPROVED, ACTIVATED
  activationDate    DateTime?
  createdAt         DateTime @default(now())
}

model GovernanceEntropySnapshot {
  id                 String   @id @default(cuid())
  workspaceId        String
  workspace          Workspace @relation(fields: [workspaceId], references: [id])
  entropyScore       Float    @default(0.0)
  amendmentVelocity  Float    @default(0.0)
  overrideFrequency  Float    @default(0.0)
  governanceDrift    String   @default("STABLE") // STABLE, DRIFTING, FRAGMENTING, ENTROPIC
  capturedAt         DateTime @default(now())
}

model InstitutionalDoctrine {
  id                 String   @id @default(cuid())
  workspaceId        String
  workspace          Workspace @relation(fields: [workspaceId], references: [id])
  doctrineType       String
  doctrineDefinition String   // JSON
  historicalEvidence String
  stabilityImpact    Float
  createdAt          DateTime @default(now())
}

model OrganizationalLineage {
  id                    String   @id @default(cuid())
  workspaceId           String
  workspace             Workspace @relation(fields: [workspaceId], references: [id])
  operationalEra        String
  resilienceScore       Float
  governancePattern     String
  treasurySurvivability Float
  recordedAt            DateTime @default(now())
}

model OperationalContinuitySnapshot {
  id                     String   @id @default(cuid())
  workspaceId            String
  workspace              Workspace @relation(fields: [workspaceId], references: [id])
  civilizationIndex      Float
  survivabilityHorizon   String
  continuityClass        String   // EMERGING, STABILIZED, RESILIENT, ENDURING, CIVILIZATIONAL
  institutionalStability Float
  createdAt              DateTime @default(now())
}

model ReliabilityTreaty {
  id                         String   @id @default(cuid())
  workspaceId                String
  workspace                  Workspace @relation(fields: [workspaceId], references: [id])
  treatyType                 String
  treatyDefinition           String   // JSON
  participatingOrganizations String   // JSON array of org/workspace IDs
  enforcementLevel           String   // BLOCKING, ADVISORY
  createdAt                  DateTime @default(now())
}

model SovereignFederation {
  id                        String   @id @default(cuid())
  federationName            String
  memberOrganizations       String   // JSON array
  federationStability       Float    @default(100.0)
  treatyCount               Int      @default(0)
  treasuryCoordinationLevel String   // ISOLATED, POOLED, BALANCED
  createdAt                 DateTime @default(now())

  civilizationCoherenceSnapshots CivilizationCoherenceSnapshot[]
  survivabilityQuarantineZones   SurvivabilityQuarantineZone[]
}

model EcosystemDependency {
  id                      String   @id @default(cuid())
  workspaceId             String
  workspace               Workspace @relation(fields: [workspaceId], references: [id])
  dependencyType          String   // CLOUD, VENDOR, ALLIANCE
  dependencyConcentration Float
  fragilityScore          Float
  survivabilityImpact     String
  updatedAt               DateTime @updatedAt
}

model OperationalDiplomacyEvent {
  id                 String   @id @default(cuid())
  federationId       String
  // Relation to SovereignFederation conceptually, simplified for now
  conflictType       String
  resolutionStrategy String
  stabilityImpact    Float
  resolvedAt         DateTime @default(now())
}

model InfrastructureSurvivabilitySnapshot {
  id                          String   @id @default(cuid())
  workspaceId                 String
  workspace                   Workspace @relation(fields: [workspaceId], references: [id])
  region                      String
  survivabilityScore          Float
  infrastructureConcentration Float
  energyReliability           Float
  capturedAt                  DateTime @default(now())
}

model EnergyGovernanceProfile {
  id                         String   @id @default(cuid())
  workspaceId                String
  workspace                  Workspace @relation(fields: [workspaceId], references: [id])
  region                     String
  energyStability            Float
  deploymentRestrictionLevel String   // NONE, THROTTLED, BLOCKED
  carbonAwarenessScore       Float
  updatedAt                  DateTime @updatedAt
}

model ClimateExposureZone {
  id                     String   @id @default(cuid())
  workspaceId            String
  workspace              Workspace @relation(fields: [workspaceId], references: [id])
  region                 String
  climateRiskType        String   // HEATWAVE, FLOOD, WILDFIRE, SEISMIC
  resilienceScore        Float
  infrastructureExposure Float
  updatedAt              DateTime @updatedAt
}

model HardwareLifecycleRecord {
  id                  String   @id @default(cuid())
  workspaceId         String
  workspace           Workspace @relation(fields: [workspaceId], references: [id])
  infrastructureClass String   // COMPUTE, STORAGE, NETWORK, COOLING
  lifecycleHealth     Float
  maintenanceDebt     Float
  survivabilityImpact String   // LOW, MODERATE, HIGH, CRITICAL
  recordedAt          DateTime @default(now())
}

model TemporalDriftSnapshot {
  id                    String   @id @default(cuid())
  workspaceId           String
  workspace             Workspace @relation(fields: [workspaceId], references: [id])
  driftScore            Float
  survivabilityDecay    Float
  governanceErosionRate Float
  recordedAt            DateTime @default(now())
}

model TemporalDebtProfile {
  id                       String   @id @default(cuid())
  workspaceId              String
  workspace                Workspace @relation(fields: [workspaceId], references: [id])
  debtScore                Float
  deferredResilienceBurden Float
  modernizationLiability   Float
  updatedAt                DateTime @updatedAt
}

model CivilizationEvolutionRecord {
  id                   String   @id @default(cuid())
  workspaceId          String
  workspace            Workspace @relation(fields: [workspaceId], references: [id])
  operationalEpoch     String
  resilienceTransition String
  survivabilityImpact  Float
  recordedAt           DateTime @default(now())
}

model FutureContinuityProjection {
  id                   String   @id @default(cuid())
  workspaceId          String
  workspace            Workspace @relation(fields: [workspaceId], references: [id])
  projectionHorizon    String   // 5_YEARS, 10_YEARS, 20_YEARS
  survivabilityForecast Float
  continuityClass      String   // REACTIVE, STABILIZING, ENDURING, MULTI_GENERATIONAL, TEMPORAL_CIVILIZATION
  generatedAt          DateTime @default(now())
}

model SystemicAwarenessSnapshot {
  id                 String   @id @default(cuid())
  workspaceId        String
  workspace          Workspace @relation(fields: [workspaceId], references: [id])
  awarenessScore     Float
  coherenceIntegrity Float
  causalityDensity   Float
  capturedAt         DateTime @default(now())
}

model OperationalPerceptionRecord {
  id                   String   @id @default(cuid())
  workspaceId          String
  workspace            Workspace @relation(fields: [workspaceId], references: [id])
  integrityScore       Float
  blindSpotIndex       Float
  synchronizationDrift Float
  recordedAt           DateTime @default(now())
}

model CivilizationCoherenceSnapshot {
  id                    String   @id @default(cuid())
  federationId          String
  federation            SovereignFederation @relation(fields: [federationId], references: [id])
  coherenceScore        Float
  continuityAlignment   Float
  survivabilityAgreement Float
  capturedAt            DateTime @default(now())
}

model ConsciousnessProjection {
  id                   String   @id @default(cuid())
  workspaceId          String
  workspace            Workspace @relation(fields: [workspaceId], references: [id])
  projectionHorizon    String
  consciousnessClass   String
  awarenessStability   Float
  generatedAt          DateTime @default(now())
}

model OperationalPathogenEvent {
  id                    String   @id @default(cuid())
  workspaceId           String
  workspace             Workspace @relation(fields: [workspaceId], references: [id])
  pathogenType          String
  contaminationSeverity String
  propagationVelocity   Float
  detectedAt            DateTime @default(now())
}

model SurvivabilityQuarantineZone {
  id               String   @id @default(cuid())
  federationId     String
  federation       SovereignFederation @relation(fields: [federationId], references: [id])
  quarantineReason String
  restrictionLevel String
  containmentStatus String
  activatedAt      DateTime @default(now())
}

model GovernanceIntegritySnapshot {
  id                 String   @id @default(cuid())
  workspaceId        String
  workspace          Workspace @relation(fields: [workspaceId], references: [id])
  purityScore        Float
  corruptionExposure Float
  integrityConfidence Float
  capturedAt         DateTime @default(now())
}

model ContinuityAntibodyRecord {
  id                 String   @id @default(cuid())
  workspaceId        String
  workspace          Workspace @relation(fields: [workspaceId], references: [id])
  antibodyType       String
  protectionStrength Float
  historicalOrigin   String
  recordedAt         DateTime @default(now())
}

model AdaptationEvidence {
  id                     String   @id @default(cuid())
  workspaceId            String
  workspace              Workspace @relation(fields: [workspaceId], references: [id])
  adaptationType         String
  previousConfiguration  String
  optimizedConfiguration String
  mathematicalEvidence   String
  constitutionalValidation String
  createdAt              DateTime @default(now())
}

model GovernanceOptimization {
  id                   String   @id @default(cuid())
  workspaceId          String
  workspace            Workspace @relation(fields: [workspaceId], references: [id])
  optimizationCategory String
  optimizationDelta    Float
  projectedImprovement Float
  activatedAt          DateTime @default(now())
}

model ConvergenceSnapshot {
  id                   String   @id @default(cuid())
  workspaceId          String
  workspace            Workspace @relation(fields: [workspaceId], references: [id])
  convergenceScore     Float
  optimizationVelocity Float
  stabilityDelta       Float
  capturedAt           DateTime @default(now())
}

model AdaptationSimulation {
  id                 String   @id @default(cuid())
  workspaceId        String
  workspace          Workspace @relation(fields: [workspaceId], references: [id])
  simulationScenario String
  projectedOutcome   String
  convergenceImpact  Float
  executedAt         DateTime @default(now())
}

model OperationalKnowledge {
  id                 String   @id @default(cuid())
  workspaceId        String
  workspace          Workspace @relation(fields: [workspaceId], references: [id])
  title              String
  knowledgeType      String
  evidenceScore      Float
  doctrineVersion    String
  createdAt          DateTime @default(now())
}

model OperationalDoctrine {
  id                     String   @id @default(cuid())
  workspaceId            String
  workspace              Workspace @relation(fields: [workspaceId], references: [id])
  doctrineName           String
  version                Int
  supersedesId           String?
  constitutionalApproval String
  publishedAt            DateTime @default(now())
}

model OperationalWisdom {
  id                   String   @id @default(cuid())
  workspaceId          String
  workspace            Workspace @relation(fields: [workspaceId], references: [id])
  wisdomStatement      String
  mathematicalEvidence String
  confidenceScore      Float
  createdAt            DateTime @default(now())
}

model InstitutionalMemoryLink {
  id                   String   @id @default(cuid())
  workspaceId          String
  workspace            Workspace @relation(fields: [workspaceId], references: [id])
  sourceType           String
  sourceId             String
  targetType           String
  targetId             String
  relationship         String
  createdAt            DateTime @default(now())
}

model OperationalDecision {
  id                 String   @id @default(cuid())
  workspaceId        String
  workspace          Workspace @relation(fields: [workspaceId], references: [id])
  decisionType       String
  selectedOption     String
  decisionScore      Float
  createdAt          DateTime @default(now())

  alternatives       DecisionAlternative[]
  evidence           DecisionEvidence[]
}

model DecisionAlternative {
  id                       String   @id @default(cuid())
  decisionId               String
  decision                 OperationalDecision @relation(fields: [decisionId], references: [id])
  optionName               String
  survivabilityImpact      Float
  treasuryImpact           Float
  constitutionalCompliance String
  ranking                  Int
}

model DecisionEvidence {
  id                 String   @id @default(cuid())
  decisionId         String
  decision           OperationalDecision @relation(fields: [decisionId], references: [id])
  evidenceType       String
  sourceReference    String
  mathematicalWeight Float
  createdAt          DateTime @default(now())
}

model DecisionSimulation {
  id                  String   @id @default(cuid())
  workspaceId         String
  workspace           Workspace @relation(fields: [workspaceId], references: [id])
  scenario            String
  recommendedDecision String
  projectedOutcome    String
  simulatedAt         DateTime @default(now())
}

model OperationalExecution {
  id                 String   @id @default(cuid())
  workspaceId        String
  workspace          Workspace @relation(fields: [workspaceId], references: [id])
  decisionId         String
  executionStatus    String
  executionScore     Float
  startedAt          DateTime @default(now())
  completedAt        DateTime?

  stages             ExecutionStage[]
  rollbackPlans      RollbackPlan[]
  replays            ExecutionReplay[]

  intents            OperationalIntent[]
  verifications      AssuranceVerification[]
}

model ExecutionStage {
  id                 String   @id @default(cuid())
  executionId        String
  execution          OperationalExecution @relation(fields: [executionId], references: [id])
  stageName          String
  executionOrder     Int
  validationStatus   String
  completedAt        DateTime?
}

model RollbackPlan {
  id                 String   @id @default(cuid())
  executionId        String
  execution          OperationalExecution @relation(fields: [executionId], references: [id])
  rollbackVersion    String
  recoveryStrategy   String
  createdAt          DateTime @default(now())
}

model ExecutionReplay {
  id                 String   @id @default(cuid())
  executionId        String
  execution          OperationalExecution @relation(fields: [executionId], references: [id])
  replayChecksum     String
  replayDuration     Int
  verifiedAt         DateTime @default(now())
}

model OperationalIntent {
  id                 String   @id @default(cuid())
  workspaceId        String
  workspace          Workspace @relation(fields: [workspaceId], references: [id])
  executionId        String
  execution          OperationalExecution @relation(fields: [executionId], references: [id])
  objective          String
  expectedOutcome    String
  definedAt          DateTime @default(now())
}

model AssuranceVerification {
  id                 String   @id @default(cuid())
  executionId        String
  execution          OperationalExecution @relation(fields: [executionId], references: [id])
  assuranceScore     Float
  verificationStatus String
  completedAt        DateTime @default(now())

  evidence           AssuranceEvidence[]
}

model AssuranceEvidence {
  id                 String   @id @default(cuid())
  verificationId     String
  verification       AssuranceVerification @relation(fields: [verificationId], references: [id])
  evidenceType       String
  sourceReference    String
  evidenceWeight     Float
  createdAt          DateTime @default(now())
}

model OperationalDriftRecord {
  id                 String   @id @default(cuid())
  workspaceId        String
  workspace          Workspace @relation(fields: [workspaceId], references: [id])
  driftCategory      String
  severity           String
  detectedAt         DateTime @default(now())
}

model OperationalOptimization {
  id                      String   @id @default(cuid())
  workspaceId             String
  workspace               Workspace @relation(fields: [workspaceId], references: [id])
  optimizationObjective   String
  optimizationScore       Float
  selectedConfiguration   String
  createdAt               DateTime @default(now())

  constraints             OptimizationConstraint[]
  candidates              OptimizationCandidate[]
  simulations             OptimizationSimulation[]
}

model OptimizationConstraint {
  id                 String   @id @default(cuid())
  optimizationId     String
  optimization       OperationalOptimization @relation(fields: [optimizationId], references: [id])
  constraintType     String
  mathematicalLimit  Float
  sourceReference    String
}

model OptimizationCandidate {
  id                       String   @id @default(cuid())
  optimizationId           String
  optimization             OperationalOptimization @relation(fields: [optimizationId], references: [id])
  candidateName            String
  objectiveScore           Float
  survivabilityImpact      Float
  constitutionalCompliance Boolean
  ranking                  Int
}

model OptimizationSimulation {
  id                     String   @id @default(cuid())
  workspaceId            String
  workspace              Workspace @relation(fields: [workspaceId], references: [id])
  optimizationId         String
  optimization           OperationalOptimization @relation(fields: [optimizationId], references: [id])
  projectedImprovement   String
  simulationResult       String
  simulatedAt            DateTime @default(now())
}

model OperationalPlan {
  id                   String   @id @default(cuid())
  workspaceId          String
  workspace            Workspace @relation(fields: [workspaceId], references: [id])
  planName             String
  planningObjective    String
  planningScore        Float
  status               String
  createdAt            DateTime @default(now())

  milestones           PlanMilestone[]
  dependencies         PlanDependency[]
  simulations          PlanSimulation[]
}

model PlanMilestone {
  id                   String   @id @default(cuid())
  planId               String
  plan                 OperationalPlan @relation(fields: [planId], references: [id])
  milestoneName        String
  executionOrder       Int
  completionStatus     String
  targetDate           DateTime?

  sourceDependencies   PlanDependency[] @relation("SourceMilestone")
  targetDependencies   PlanDependency[] @relation("TargetMilestone")
}

model PlanDependency {
  id                   String   @id @default(cuid())
  planId               String
  plan                 OperationalPlan @relation(fields: [planId], references: [id])
  
  sourceMilestoneId    String
  sourceMilestone      PlanMilestone @relation("SourceMilestone", fields: [sourceMilestoneId], references: [id])
  
  targetMilestoneId    String
  targetMilestone      PlanMilestone @relation("TargetMilestone", fields: [targetMilestoneId], references: [id])
  
  dependencyType       String
}

model PlanSimulation {
  id                      String   @id @default(cuid())
  workspaceId             String
  workspace               Workspace @relation(fields: [workspaceId], references: [id])
  planId                  String
  plan                    OperationalPlan @relation(fields: [planId], references: [id])
  projectedCompletion     DateTime
  survivabilityProjection Float
  simulatedAt             DateTime @default(now())
}

model OperationalCoordination {
  id                   String   @id @default(cuid())
  workspaceId          String
  workspace            Workspace @relation(fields: [workspaceId], references: [id])
  coordinationName     String
  coordinationScore    Float
  coordinationStatus   String
  createdAt            DateTime @default(now())

  nodes                CoordinationNode[]
  conflicts            CoordinationConflict[]
  resourceReservations ResourceReservation[]
}

model CoordinationNode {
  id                   String   @id @default(cuid())
  coordinationId       String
  coordination         OperationalCoordination @relation(fields: [coordinationId], references: [id])
  nodeType             String
  nodeReference        String
  operationalRole      String
}

model CoordinationConflict {
  id                   String   @id @default(cuid())
  coordinationId       String
  coordination         OperationalCoordination @relation(fields: [coordinationId], references: [id])
  conflictType         String
  severity             String
  resolutionStrategy   String
  resolvedAt           DateTime @default(now())
}

model ResourceReservation {
  id                   String   @id @default(cuid())
  coordinationId       String
  coordination         OperationalCoordination @relation(fields: [coordinationId], references: [id])
  resourceType         String
  reservedCapacity     Float
  reservationWindow    String
  createdAt            DateTime @default(now())
}

model OperationalIntelligenceSnapshot {
  id                   String   @id @default(cuid())
  workspaceId          String
  workspace            Workspace @relation(fields: [workspaceId], references: [id])
  intelligenceScore    Float
  organizationalHealth Float
  intelligenceClass    String
  createdAt            DateTime @default(now())

  signals              OperationalSignal[]
  correlations         IntelligenceCorrelation[]
}

model OperationalSignal {
  id                   String   @id @default(cuid())
  snapshotId           String
  snapshot             OperationalIntelligenceSnapshot @relation(fields: [snapshotId], references: [id])
  signalName           String
  signalValue          Float
  signalCategory       String
}

model IntelligenceCorrelation {
  id                   String   @id @default(cuid())
  snapshotId           String
  snapshot             OperationalIntelligenceSnapshot @relation(fields: [snapshotId], references: [id])
  sourceSubsystem      String
  targetSubsystem      String
  correlationReason    String
  evidenceReference    String
}

model IntelligenceSimulation {
  id                   String   @id @default(cuid())
  workspaceId          String
  workspace            Workspace @relation(fields: [workspaceId], references: [id])
  scenario             String
  projectedHealth      Float
  survivabilityDelta   Float
  simulatedAt          DateTime @default(now())
}

model IntegrationConnector {
  id                   String   @id @default(cuid())
  workspaceId          String
  workspace            Workspace @relation(fields: [workspaceId], references: [id])
  provider             String
  status               String
  lastSync             DateTime?
  createdAt            DateTime @default(now())

  events               IntegrationEvent[]
  healthSnapshots      IntegrationHealthSnapshot[]
}

model IntegrationEvent {
  id                   String   @id @default(cuid())
  workspaceId          String
  workspace            Workspace @relation(fields: [workspaceId], references: [id])
  connectorId          String
  connector            IntegrationConnector @relation(fields: [connectorId], references: [id])
  eventType            String
  externalId           String
  payloadHash          String
  validationState      String
  createdAt            DateTime @default(now())

  synchronization      IntegrationSynchronization?
}

model IntegrationSynchronization {
  id                   String   @id @default(cuid())
  workspaceId          String
  workspace            Workspace @relation(fields: [workspaceId], references: [id])
  eventId              String   @unique
  event                IntegrationEvent @relation(fields: [eventId], references: [id])
  targetSubsystem      String
  synchronizationStatus String
  syncedAt             DateTime @default(now())
}

model IntegrationHealthSnapshot {
  id                   String   @id @default(cuid())
  workspaceId          String
  workspace            Workspace @relation(fields: [workspaceId], references: [id])
  connectorId          String
  connector            IntegrationConnector @relation(fields: [connectorId], references: [id])
  uptimePercentage     Float
  latencyMs            Int
  status               String
  createdAt            DateTime @default(now())
}

model EnterpriseRelease {
  id                   String   @id @default(cuid())
  workspaceId          String
  workspace            Workspace @relation(fields: [workspaceId], references: [id])
  releaseName          String
  status               String
  readinessScore       Float
  governanceStatus     String
  createdAt            DateTime @default(now())
  updatedAt            DateTime @updatedAt

  approvals            ReleaseApproval[]
  checkpoints          ReleaseCheckpoint[]
  evidence             ReleaseEvidence[]
  releaseType String?
}

model ReleaseApproval {
  id                   String   @id @default(cuid())
  releaseId            String
  release              EnterpriseRelease @relation(fields: [releaseId], references: [id])
  approverRole         String
  status               String
  approvedAt           DateTime?
  comments             String?
}

model ReleaseCheckpoint {
  id                   String   @id @default(cuid())
  releaseId            String
  release              EnterpriseRelease @relation(fields: [releaseId], references: [id])
  milestone            String
  status               String
  reachedAt            DateTime?
}

model ReleaseEvidence {
  id                   String   @id @default(cuid())
  releaseId            String
  release              EnterpriseRelease @relation(fields: [releaseId], references: [id])
  evidenceType         String
  referenceId          String
  validationHash       String
  recordedAt           DateTime @default(now())
  workspaceId String?
}

model ReleaseVersion {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data String?
  createdAt   DateTime @default(now())
}

model ReleaseArtifact {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data String?
  createdAt   DateTime @default(now())
}

model DeploymentPlan {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data String?
  createdAt   DateTime @default(now())
}

model DeploymentExecution {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data String?
  createdAt   DateTime @default(now())
}

model DeploymentEnvironment {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data String?
  createdAt   DateTime @default(now())
}

model DeploymentTarget {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data String?
  createdAt   DateTime @default(now())
}

model DeploymentWindow {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data String?
  createdAt   DateTime @default(now())
}

model DeploymentApproval {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data String?
  createdAt   DateTime @default(now())
}

model DeploymentValidation {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data String?
  createdAt   DateTime @default(now())
}

model ProgressiveRollout {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data String?
  createdAt   DateTime @default(now())
}

model RolloutStage {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data String?
  createdAt   DateTime @default(now())
}

model DeploymentCheckpoint {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data String?
  createdAt   DateTime @default(now())
}

model ProductionVerification {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data String?
  createdAt   DateTime @default(now())
}

model ReleaseAssessment {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data String?
  createdAt   DateTime @default(now())
}

model ReleaseSnapshot {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data String?
  createdAt   DateTime @default(now())
}

model ReleaseTimeline {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data String?
  createdAt   DateTime @default(now())
}

model ReleaseLedger {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data String?
  createdAt   DateTime @default(now())
}

model ReleaseIndex {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  score Float
  createdAt   DateTime @default(now())
}

model DeploymentHealth {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data String?
  createdAt   DateTime @default(now())
}

model EnterpriseApi {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ApiConsumer {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ApiProvider {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ChangeApproval {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  changeId String?
  approver String?
  role String?
  decision String?
}

model ChangeCalendarEvent {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ChangeImpactAssessment {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  changeId String?
  affectedServices String?
  affectedRegions String?
  dependencyCount String?
}

model OperationalChange {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  status String?
  title String?
  description String?
  category String?
  priority String?
  requester String?
  owner String?
}

model EnterpriseEvent {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model GovernanceAssessment {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model PolicyException {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model GovernancePolicy {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model RiskRegister {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model OperationalIncident {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  status String?
  startTime String?
}

model StrategicInitiative {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model OperationalInsight {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model KnowledgeCoverage {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model TelemetryRetention {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ServiceCatalog {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ResourcePolicy {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  targetResource String?
}

model ProvisioningRequest {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  requester String?
  requestType String?
  approvalStatus String?
  executionStatus String?
}

model PlatformResource {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model PortfolioObjective {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ErrorBudgetLedger {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  sloId String?
  timestamp DateTime?
}

model ReliabilityForecast {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  sloId String?
}

model ServiceLevelIndicator {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  sloId String?
  timestamp DateTime?
}

model ServiceLevelObjective {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  serviceId String?
  name String?
  availabilityTarget String?
  latencyTarget String?
  errorRateTarget String?
  measurementWindow String?
  status String?
}

model ServiceApproval {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  request String?
}

model ServiceRequest {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ServiceKnowledge {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ServiceQueue {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model BusinessCapability {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model Microservice {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model TopologyNode {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model TopologyEdge {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model OperationalWorkflow {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ApiContract {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ApiIndex {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ApiLedger {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ApiEndpoint {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model IntegrationFlow {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model EventChannel {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model MessageContract {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ApiPolicy {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model SchemaRegistry {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ApiSubscription {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model WebhookEndpoint {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ConfigurationItem {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  updatedAt String?
}

model AssetRelationship {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ConfigurationSnapshot {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ServiceTopology {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model DataClassification {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model EnterpriseDataset {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model DataRetentionPolicy {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model DataLifecycle {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model DataLineage {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model MasterDataEntity {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model MetadataCatalog {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model MetadataIndex {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model MetadataLedger {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model DataOwner {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model DataQualityRule {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ReferenceDataEntity {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model SimulationCheckpoint {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model TwinHealth {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model DigitalTwinLedger {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  timestamp DateTime?
}

model TwinSimulationExecution {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  status String?
}

model SimulationScenario {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model SimulationEvidence {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model FailureScenario {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ImpactAssessment {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model Phase134RecoveryScenario {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ReplaySession {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model StateTransition {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model TwinState {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ValidationResult {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model EventConsumer {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model DeliveryPolicy {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model EventTopic {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model EventQueue {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model EventStream {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model EventProducer {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model RetryPolicy {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model RoutingPolicy {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model StreamingIndex {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model StreamingLedger {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model EventSubscription {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model BudgetPolicy {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  updatedAt String?
}

model CloudCostRecord {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model FinOpsSnapshot {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ResourceOptimization {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  priority String?
}

model AccessPolicy {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model AccessReview {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model AccessRequest {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  status String?
}

model ApprovalWorkflow {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ApiCredential {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model EmergencyAccess {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model EnterpriseIdentity {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model EnterpriseRole {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model PrivilegedSession {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  status String?
}

model IdentityLedger {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model IdentityProvider {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model IdentityRisk {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model Permission {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ServiceAccount {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model SeparationOfDutyRule {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model IncidentTimelineEvent {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  incidentId String?
  timestamp DateTime?
}

model IncidentRecoveryPlan {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  incidentId String?
}

model RootCauseEvidence {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  incidentId String?
}

model BusinessImpact {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model BusinessObjective {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model OperationalCorrelation {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model DecisionPackage {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model DecisionLedger {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  timestamp DateTime?
}

model EvidenceCorrelation {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ExecutiveBriefing {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ExecutiveKPI {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model EnterpriseHealth {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model EnterpriseMetric {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model DepartmentScorecard {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model StrategicRisk {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model OperationalTrend {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ArchitectureDecision {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model DecisionMemory {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model KnowledgeReview {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  status String?
}

model KnowledgeGap {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model KnowledgeArticle {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model KnowledgeDomain {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model KnowledgeCategory {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model KnowledgeEvidence {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model KnowledgeGraphNode {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model KnowledgeGraphEdge {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model KnowledgeIndex {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model KnowledgeLedger {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model PlaybookKnowledge {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model KnowledgeRelationship {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model RunbookKnowledge {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model TelemetryAlert {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model TelemetryCorrelation {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model DiagnosticSession {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model TelemetryMetric {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model TelemetryLog {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model TelemetryTrace {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model HealthIndicator {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ObservabilityAssessment {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ObservabilitySnapshot {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model TelemetrySource {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model TelemetryIndex {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model TelemetryLedger {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ExecutionApproval {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ExecutionCheckpoint {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ExecutionConstraint {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model TaskDependency {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ExecutionPlan {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  status String?
}

model PipelineExecution {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  status String?
}

model ExecutionTask {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  status String?
}

model ExecutionMetrics {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ExecutionLedger {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ExecutionLock {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ExecutionPipeline {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ExecutionQueue {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model AopExecutionReplay {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ExecutionReservation {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ExecutionValidation {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model OrchestrationWorkflow {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model CapacityAllocation {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model PlanningAssessment {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model EnterpriseCapacity {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model CapacityConstraint {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model DemandRequest {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model PlanningIndex {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model PlanningEvidence {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model PlanningLedger {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model CapacityReservation {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model CapacityResource {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model PlanningScenario {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model CapacityUtilization {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model PortfolioAssessment {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model BenefitRealization {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model InitiativeDependency {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model PortfolioIndex {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model TransformationProgram {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model PortfolioInitiative {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model InvestmentAllocation {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model EnterprisePortfolio {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model PortfolioEvidence {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model PortfolioLedger {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ExecutiveRoadmap {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ValueStream {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ReleasePackage {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  releaseId String?
  artifactType String?
  artifactHash String?
  isImmutable String?
}

model ReleaseRiskProfile {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  releaseId String?
  blastRadius String?
}

model ReleaseStrategy {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  releaseId String?
  strategyType String?
  description String?
}

model ReleaseWindow {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  startTime String?
}

model BusinessContinuityPlan {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model BusinessImpactAnalysis {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model RecoveryCheckpoint {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ContinuityLedger {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model RecoveryDependency {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model RecoveryPlan {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ResilienceIndex {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model RecoveryReadiness {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model RecoveryExercise {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model FailbackPlan {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model FailoverExecution {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model Phase134RecoveryExecution {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model RecoveryValidation {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ResilienceAssessment {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ComplianceControl {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  updatedAt String?
}

model SecurityAuditSnapshot {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model SecurityFinding {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  detectionTimestamp String?
}

model SecurityPolicy {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ApplicationPortfolio {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model EnterpriseAsset {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model BlastRadiusAnalysis {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model CloudAccount {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model ServiceDependency {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  status String?
  critical String?
}

model BusinessService {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model InfrastructureResource {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model OperationalLineage {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model NetworkZone {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model TopologyLedger {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
}

model OperationalWorkflowExecution {
  id          String   @id @default(cuid())
  workspaceId String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  data        String?
  createdAt   DateTime @default(now())
  startTime String?
}

// Phase 143: Configuration Governance
model EnterpriseConfiguration {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ConfigurationDomain {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ConfigurationProfile {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ConfigurationTemplate {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 197: Enterprise Backup, Recovery, Archival & Disaster Recovery Execution Governance Platform
model BackupPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BackupExecution {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BackupSchedule {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BackupRepository {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BackupSnapshot {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RecoveryPoint {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseRecoveryExecution {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RestoreOperation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseRecoveryValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ArchiveRepository {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ArchiveRetention {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ReplicationPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ReplicationExecution {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DisasterRecoveryExecution {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseFailoverExecution {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FailbackExecution {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RecoverySite {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RecoveryEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BackupAudit {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RecoveryLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RecoveryBaseline {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model StorageProtectionProfile {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BackupConfiguration {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 198: Enterprise Licensing, Entitlements & Subscription Governance Platform
model LicenseCatalog {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LicenseProduct {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LicenseEdition {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LicenseAgreement {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LicenseKey {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LicenseAssignment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LicenseActivation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LicenseUsage {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LicenseQuota {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LicensePool {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SubscriptionPlan {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SubscriptionEntitlement {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FeatureEntitlement {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SeatAllocation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model UsageMeter {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ConsumptionRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model OveragePolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RenewalSchedule {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EntitlementPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LicenseEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LicenseAudit {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LicenseLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommercialEntitlement {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 199: Enterprise Contract Lifecycle, Agreement & Obligation Governance Platform
model EnterpriseContract {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ContractTemplate {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ContractClause {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ClauseLibrary {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ContractParty {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ContractNegotiation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ContractVersion {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ContractApproval {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ContractExecution {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ContractAmendment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ContractRenewal {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ContractTermination {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ContractObligation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ContractMilestone {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ContractDeliverable {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ContractCompliance {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ContractRisk {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ContractEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ContractRepository {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ContractSignature {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ContractLifecycle {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ContractLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ContractRetentionPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 200: Enterprise Communications, Notifications & Messaging Governance Platform
model CommunicationChannel {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommunicationTemplate {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommunicationCampaign {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model NotificationEvent {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model NotificationMessage {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model NotificationRecipient {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model NotificationSubscription {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model NotificationPreference {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DeliveryExecution {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DeliveryReceipt {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommunicationPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommunicationApproval {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommunicationSchedule {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommunicationQueue {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommunicationAttachment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommunicationEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommunicationAudit {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommunicationRetention {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommunicationArchive {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommunicationFailure {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommunicationRetry {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommunicationLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommunicationProfile {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 201: Enterprise Document, Content, Records & Document Lifecycle Governance Platform
model EnterpriseDocument {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DocumentRepository {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DocumentFolder {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DocumentCategory {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DocumentClassification {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DocumentVersion {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DocumentApproval {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DocumentWorkflow {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DocumentReview {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DocumentPublication {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DocumentDistribution {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DocumentRetention {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DocumentArchive {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DocumentDisposition {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DocumentSignature {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DocumentAccessPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DocumentEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RecordsManagementPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model OfficialRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ContentCollection {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DocumentMetadata {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DocumentAuditTrail {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DocumentLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 202: Enterprise Scheduling, Calendar, Resource & Time Coordination Governance Platform
model EnterpriseCalendar {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CalendarEvent {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SchedulingPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SchedulingWindow {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseResourceReservation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ResourceSchedule {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ResourceAvailability {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SchedulingConstraint {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TimeSlot {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkingCalendar {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model HolidayCalendar {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MaintenanceSchedule {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ExecutionSchedule {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ScheduleApproval {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ScheduleConflict {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ScheduleException {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ScheduleNotification {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RecurringSchedule {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ScheduleEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ScheduleAudit {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ScheduleLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TimeZoneProfile {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CalendarIntegration {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 203: Enterprise Geographic Information, Spatial Assets & Location Governance Platform
model GeographicRegion {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AdministrativeBoundary {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseFacility {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FacilityCampus {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model Building {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FloorPlan {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SpatialAsset {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LocationHierarchy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model GeoCoordinate {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ServiceTerritory {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SalesTerritory {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DeliveryZone {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model OperationalZone {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RestrictedArea {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model GeofencePolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LocationClassification {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SpatialRelationship {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MapLayer {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SpatialDataset {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LocationEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LocationAudit {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SpatialLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LocationReference {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ConfigurationVersion {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ConfigurationParameter {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnvironmentProfile {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnvironmentBaseline {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnvironmentSnapshot {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ConfigurationSecret {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SecretReference {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SecretProvider {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CertificateAsset {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EncryptionPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RotationPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ConfigurationApproval {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ConfigurationValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ConfigurationEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ConfigurationAssessment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ConfigurationTimeline {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ConfigurationLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ConfigurationIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ConfigurationHealth {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 144: Service Governance
model EnterpriseService {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ServiceTier {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ServiceOwner {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ServiceConsumer {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CustomerAccount {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ServiceAgreement {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ServiceLevelAgreement {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model OperationalObjective {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ServiceAvailability {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AvailabilityWindow {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ServiceDependencyMap {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CustomerJourney {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CustomerTouchpoint {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BusinessCommitment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ServiceEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ServiceAssessment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ServiceSnapshot {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ServiceTimeline {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ServiceLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ServiceIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CustomerExperienceHealth {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 145: Business Process Governance
model BusinessProcess {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProcessCategory {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProcessOwner {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model StandardOperatingProcedure {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model OperationalProcedure {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProcedureStep {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DecisionGate {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ControlPoint {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BusinessRule {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProcessDependency {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProcessExecution {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProcessEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProcessApproval {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProcessAssessment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProcessSnapshot {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProcessTimeline {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProcessLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProcessIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProcessHealth {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProcedureTemplate {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProcessCompliance {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProcessException {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 146: Compliance Governance
model ComplianceFramework {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model Regulation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ComplianceRequirement {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ControlObjective {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ControlTest {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ControlExecution {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ControlEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ControlAssessment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ComplianceAssessment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ComplianceCertification {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AuditProgram {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AuditEngagement {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AuditFinding {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AuditRecommendation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CorrectiveAction {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RemediationPlan {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ComplianceException {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ComplianceSnapshot {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ComplianceTimeline {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ComplianceLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ComplianceIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AuditHealth {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 147: Architecture Governance
model EnterpriseArchitectureDomain {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ArchitectureCapability {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BusinessCapabilityMap {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ArchitecturePrinciple {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ArchitectureStandard {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TechnologyStandard {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ApprovedTechnology {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TechnologyLifecycle {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TechnologyException {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ReferenceArchitecture {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SolutionArchitecture {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ArchitectureReview {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ArchitectureDecisionRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ArchitectureAssessment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ArchitectureRisk {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TechnicalDebtItem {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ModernizationInitiative {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TechnologyRoadmap {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 191: Enterprise Knowledge, Policy, Standards & Intellectual Capital Governance Platform
model KnowledgeRepository {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseKnowledgeDomain {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseKnowledgeArticle {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model KnowledgeCollection {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseKnowledgeCategory {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model KnowledgeClassification {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseOperationalProcedure {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseStandardOperatingProcedure {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterprisePolicyDocument {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseStandard {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TechnicalGuideline {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EngineeringHandbook {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model OperationsPlaybook {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseKnowledgeReview {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model KnowledgeApproval {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model KnowledgeVersion {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseKnowledgeRelationship {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model KnowledgeReference {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseKnowledgeEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseKnowledgeLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model KnowledgeRetentionPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model InstitutionalMemory {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model IntellectualCapitalAsset {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 192: Enterprise Automation, Robotics & Orchestration Governance Platform
model AutomationProgram {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AutomationPortfolio {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AutomationDefinition {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AutomationWorkflow {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AutomationTrigger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AutomationSchedule {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AutomationExecution {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AutomationStep {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RobotWorker {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RobotPool {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RobotAssignment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AutomationQueue {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AutomationCredential {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AutomationApproval {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AutomationException {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AutomationEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AutomationLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AutomationDependency {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AutomationPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AutomationExecutionPlan {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AutomationConfiguration {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AutomationLifecycle {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AutomationHealth {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 193: Enterprise Identity, Directory, Access & Federation Governance Platform
model EnterpriseEnterpriseIdentity {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model IdentityDirectory {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DirectoryDomain {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseIdentityProvider {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AuthenticationMethod {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FederationTrust {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FederationProvider {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model IdentitySession {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseServiceAccount {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PrivilegedAccount {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AccessCredential {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CredentialVault {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CredentialRotation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model IdentityLifecycle {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model IdentityProvisioning {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model IdentityDeprovisioning {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model IdentityEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseIdentityLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AccessCertification {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model IdentityPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AuthenticationEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AuthorizationEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model IdentityGovernanceProfile {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 194: Enterprise Data Platform, Lakehouse & Data Engineering Governance Platform
model DataLake {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DataWarehouse {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseLakehouse {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DataPipeline {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DataPipelineExecution {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DataTransformation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model Dataset {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DatasetVersion {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DataContract {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DataProduct {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DataCatalogEntry {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseDataLineage {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DataLineageNode {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model StreamingPipeline {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BatchPipeline {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ChangeDataCapturePipeline {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseDataQualityRule {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DataValidationExecution {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DataRetentionProfile {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DataMeshDomain {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DataFabricNode {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DataEngineeringEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DataEngineeringLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 195: Enterprise API Management, Gateway & API Governance Platform
model EnterpriseAPI {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model APIService {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model APIEndpoint {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model APIVersion {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model APIContract {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model APISpecification {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model APIConsumer {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model APIApplication {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model APISubscription {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model APIKey {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model APIGateway {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model APIPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model APIRoute {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model APIRateLimit {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model APIToken {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model APICertificate {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model APILifecycle {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model APIDeployment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model APITelemetry {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model APIAccessLog {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model APIEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model APILedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model APIPortal {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 196: Enterprise Configuration, Feature Flag & Runtime Configuration Governance Platform
model EnterpriseConfigurationProfile {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RuntimeConfiguration {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnvironmentConfiguration {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ConfigurationVariable {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ConfigurationGroup {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseConfigurationVersion {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseConfigurationSnapshot {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ConfigurationBaseline {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FeatureFlag {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FeatureFlagRule {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FeatureFlagRollout {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ConfigurationPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseConfigurationApproval {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ConfigurationDeployment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ConfigurationOverride {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TenantConfiguration {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseSecretReference {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ConfigurationAudit {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseConfigurationEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseConfigurationLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RuntimeProfile {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RolloutWindow {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseConfigurationTemplate {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ArchitectureDependency {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ArchitectureEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ArchitectureValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ArchitectureLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ArchitectureIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 148: Enterprise Project Delivery & Execution Governance
model EnterpriseProject {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProjectWorkspace {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProjectLifecycle {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProjectPhase {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProjectMilestone {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkPackage {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProjectTask {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProjectDependency {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CriticalPath {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model Deliverable {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DeliverableEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProjectCheckpoint {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProjectApproval {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProjectRisk {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProjectIssue {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProjectDecision {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProjectSchedule {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ResourceAssignment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProjectBaseline {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProjectSnapshot {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProjectValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProjectLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProjectHealthIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 149: Enterprise Financial Governance
model FinancialOrganization {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FiscalYear {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FiscalPeriod {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model Budget {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BudgetCategory {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BudgetAllocation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CostCenter {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProfitCenter {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CapitalExpenditure {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model OperationalExpenditure {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FinancialRequest {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FinancialApproval {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FinancialCommitment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FinancialObligation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ExpensePolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SpendingLimit {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ForecastScenario {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BudgetVariance {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FinancialControl {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FinancialEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FinancialValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FinancialLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FinancialHealthIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 150: Enterprise Vendor, Supplier & Third-Party Governance
model VendorOrganization {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model Supplier {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ThirdPartyProvider {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model VendorCategory {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model VendorService {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model VendorContract {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseContractAmendment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProcurementRequest {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProcurementApproval {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model VendorAssessment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model VendorRisk {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model VendorCompliance {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model VendorCertification {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ServiceCommitment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ServicePerformance {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model VendorReview {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model VendorRenewal {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model VendorTermination {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model VendorEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model VendorValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model VendorLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model VendorRelationship {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model VendorHealthIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 151: Enterprise Asset Lifecycle, Inventory & Maintenance Governance
model EnterpriseLifecycleAsset {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AssetCategory {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AssetClassification {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AssetLocation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AssetOwner {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AssetLifecycle {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AssetAcquisition {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AssetCommissioning {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AssetInventory {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AssetAssignment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AssetMaintenancePlan {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseMaintenanceSchedule {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MaintenanceExecution {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CalibrationRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model InspectionRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AssetWarranty {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AssetDepreciation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AssetTransfer {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AssetRetirement {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AssetDisposal {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AssetEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AssetValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AssetLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 152: Enterprise Workforce, Organization & Human Capital Governance
model WorkforceOrganization {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BusinessUnit {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model Department {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model Team {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model Position {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkforceMember {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EmploymentRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model OrganizationAssignment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ReportingRelationship {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkforceRole {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SkillCatalog {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkforceSkill {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkforceCertification {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TrainingRequirement {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkforceCompetency {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkforceCapacity {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkforceAllocation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SuccessionPlan {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkforcePolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkforceEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkforceValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkforceLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkforceHealthIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 153: Enterprise Procurement, Strategic Sourcing & Purchasing Governance
model ProcurementOrganization {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProcurementPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProcurementCategory {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model StrategicSourcingProgram {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SourcingEvent {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RequestForInformation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RequestForProposal {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RequestForQuotation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseProcurementRequest {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PurchaseRequisition {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PurchaseOrder {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PurchaseOrderLine {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PurchaseApproval {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProcurementCommittee {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BidSubmission {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BidEvaluation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model VendorSelection {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model GoodsReceipt {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProcurementException {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProcurementEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProcurementValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProcurementLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProcurementHealthIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 154: Enterprise Supply Chain, Logistics & Inventory Governance
model SupplyChainNetwork {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DistributionCenter {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model Warehouse {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WarehouseZone {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model StorageLocation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model InventoryItem {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model InventoryLot {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model InventoryBatch {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model InventoryTransaction {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model StockReservation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model StockTransfer {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ReplenishmentPlan {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DemandPlan {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model Shipment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ShipmentRoute {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model Carrier {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model Delivery {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LogisticsGoodsReceipt {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model InventoryAudit {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SupplyChainEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SupplyChainValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SupplyChainLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SupplyChainHealthIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 155: Enterprise Manufacturing, Production & Shop Floor Governance
model ManufacturingPlant {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductionLine {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkCenter {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ManufacturingCell {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BillOfMaterial {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BillOfMaterialRevision {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductionRouting {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ManufacturingOrder {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ManufacturingOperation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkInstruction {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductionBatch {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MaterialConsumption {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MaterialIssue {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FinishedGoodsReceipt {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MachineAssignment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductionSchedule {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductionCheckpoint {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductionDowntime {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductionTraceability {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ManufacturingEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ManufacturingValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ManufacturingLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ManufacturingHealthIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 156: Enterprise Quality Management, Inspection & Continuous Improvement
model QualityManagementSystem {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model QualityPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model QualityObjective {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model QualityInspectionPlan {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model QualityInspection {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model InspectionCheckpoint {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model QualityTest {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TestSpecification {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TestResult {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model NonConformanceRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model QualityCorrectiveAction {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PreventiveAction {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RootCauseAnalysis {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model QualityDeviation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model QualityAudit {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SupplierQualityAssessment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductQualityRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProcessQualityRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ContinuousImprovementInitiative {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model QualityEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model QualityValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model QualityLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model QualityHealthIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 157: Enterprise Product Lifecycle, Engineering Change & Innovation Governance Platform
model ProductPortfolio {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductFamily {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseProduct {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductVariant {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductConfiguration {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductRequirement {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductSpecification {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductRevision {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EngineeringBaseline {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EngineeringChangeRequest {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EngineeringChangeOrder {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EngineeringReview {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductBillOfMaterial {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductStructure {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductRelease {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductLifecycleState {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductInnovationInitiative {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductRoadmap {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductComplianceMatrix {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductHealthIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 158: Enterprise Customer, CRM & Revenue Operations Governance Platform
model CustomerOrganization {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CRMClientAccount {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CustomerContact {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CustomerHierarchy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CustomerSegment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseSalesTerritory {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SalesPipeline {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SalesOpportunity {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SalesQuotation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PricingAgreement {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CustomerContract {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CustomerSubscription {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CustomerOnboarding {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CustomerSuccessPlan {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CustomerRenewal {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CustomerEscalation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RevenueForecast {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RevenueCommitment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CustomerEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CustomerValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CustomerLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CustomerComplianceProfile {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CustomerHealthIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 159: Enterprise Sales, Go-To-Market & Commercial Governance Platform
model SalesOrganization {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SalesRegion {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommercialTerritory {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TerritoryAssignment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommercialAccountPlan {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SalesCampaign {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LeadQualification {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommercialOpportunity {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DealPipeline {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DealDesk {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PricingPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DiscountApproval {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommercialProposal {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model QuoteApproval {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SalesQuota {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SalesTarget {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SalesForecast {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ChannelPartner {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PartnerProgram {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommercialEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommercialValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommercialLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommercialHealthIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 160: Enterprise Legal, Contract Lifecycle & Corporate Governance Platform
model LegalEntity {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CorporateStructure {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CorporateResolution {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BoardMeeting {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BoardResolution {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseContractTemplate {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseEnterpriseContract {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseContractClause {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ContractRevision {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseContractApproval {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseContractObligation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LegalMatter {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LitigationCase {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model IntellectualPropertyAsset {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TrademarkRegistry {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PatentRegistry {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PowerOfAttorney {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PolicyAcknowledgement {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LegalEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LegalValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LegalLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LegalComplianceMatrix {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LegalHealthIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 161: Enterprise Document, Records & Information Governance Platform
model EnterpriseEnterpriseDocument {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DocumentLibrary {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseDocumentCategory {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DocumentTemplate {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ControlledDocument {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseDocumentVersion {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseDocumentApproval {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DocumentReviewCycle {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RecordsClassification {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RecordsRetentionPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RecordsArchive {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LegalHold {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model InformationAsset {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model InformationClassification {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model InformationCustodian {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RecordsDisposition {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseDocumentDistribution {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DocumentAcknowledgement {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model InformationEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model InformationValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model InformationLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model InformationComplianceMatrix {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model InformationHealthIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 162: Enterprise Privacy, Data Protection & Consent Governance Platform
model PrivacyPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PrivacyNotice {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PersonalDataCategory {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SensitiveDataCategory {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DataSubject {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ConsentRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ConsentPurpose {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ConsentWithdrawal {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProcessingActivity {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProcessingPurpose {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DataController {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DataProcessor {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DataTransferAgreement {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CrossBorderTransfer {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DataSubjectRequest {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PrivacyImpactAssessment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DataProtectionAssessment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PrivacyIncident {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PrivacyEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PrivacyValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PrivacyLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PrivacyComplianceMatrix {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PrivacyHealthIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 163: Enterprise Risk Management, Operational Risk & Business Continuity Governance Platform
model EnterpriseRisk {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RiskCategory {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseRiskRegister {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RiskAssessment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RiskExposure {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RiskTreatmentPlan {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RiskMitigationAction {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RiskAcceptance {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RiskException {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model KeyRiskIndicator {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RiskEscalation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseBusinessContinuityPlan {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RecoveryStrategy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseRecoveryExercise {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DisasterRecoveryScenario {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CrisisManagementEvent {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ContinuityDependency {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ContinuityValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RiskEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RiskValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RiskLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RiskComplianceMatrix {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RiskHealthIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 164: Enterprise Security Operations, Cyber Defense & Zero Trust Governance Platform
model EnterpriseSecurityPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SecurityControl {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ZeroTrustPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SecurityZone {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SecurityAsset {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SecurityIncident {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SecurityEvent {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ThreatIndicator {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ThreatCampaign {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model VulnerabilityRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model VulnerabilityAssessment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PatchCompliance {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EndpointSecurityProfile {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model NetworkSecurityPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model IdentityProtectionPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PrivilegedAccessSession {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CryptographicKeyRegistry {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SecurityException {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SecurityEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SecurityValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SecurityLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SecurityComplianceMatrix {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SecurityHealthIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 165: Enterprise AI Governance, Model Risk & Intelligent Systems Oversight Platform
model EnterpriseAIRegistry {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AIProvider {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AIModel {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AIModelVersion {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AIEndpoint {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AIDeployment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AIApproval {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AIUsagePolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AIModelRisk {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AIModelInventory {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AIEvaluation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AIBenchmark {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AIBiasAssessment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AIExplainabilityRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AIRegulatoryRequirement {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AIGovernanceEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AIAuditTrail {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AIControl {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AIException {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AIValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AILedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AIComplianceMatrix {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AIHealthIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 166: Enterprise Sustainability, ESG & Corporate Responsibility Governance Platform
model SustainabilityProgram {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ESGFramework {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ESGObjective {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ESGMetric {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SustainabilityTarget {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CarbonInventory {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model GreenhouseGasEmission {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnergyConsumptionRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WaterConsumptionRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WasteManagementRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BiodiversityInitiative {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ClimateRiskAssessment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EthicalSourcingProgram {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SocialImpactInitiative {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommunityInvestment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SustainabilityDisclosure {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ESGAuditEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ESGValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ESGControl {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ESGLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ESGComplianceMatrix {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ESGReportingCycle {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ESGHealthIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 167: Enterprise Facilities, Real Estate & Physical Infrastructure Governance Platform
model EnterpriseCampus {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseBuilding {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FacilityZone {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseFloorPlan {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseWorkspace {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MeetingSpace {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DataCenterFacility {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model UtilitySystem {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model HVACSystem {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ElectricalInfrastructure {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WaterInfrastructure {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FacilityMaintenancePlan {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FacilityInspection {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model OccupancyRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SpaceAllocation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkplaceReservation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FacilityProject {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FacilityEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FacilityValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FacilityLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FacilityComplianceMatrix {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FacilityServiceRequest {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FacilityHealthIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 168: Enterprise Digital Workplace, Collaboration & Productivity Governance Platform
model DigitalWorkspace {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CollaborationPlatform {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CollaborationWorkspace {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CollaborationChannel {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseMeeting {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MeetingRecording {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MeetingPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseEnterpriseCalendar {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductivityPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CollaborationMembership {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkspacePermission {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CollaborationIntegration {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CollaborationRetentionPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CollaborationArchive {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DigitalNotice {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model KnowledgeSpace {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductivityMetric {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CollaborationEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CollaborationValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CollaborationLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CollaborationComplianceMatrix {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CollaborationAuditRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CollaborationHealthIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 169: Enterprise Business Intelligence, Analytics & Decision Support Governance Platform
model BusinessIntelligenceWorkspace {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AnalyticsCatalog {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DashboardDefinition {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DashboardCertification {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ExecutiveDashboard {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ReportDefinition {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ReportPublication {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SemanticModel {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DataCube {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model KPIRegistry {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MetricDefinition {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model Scorecard {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AnalyticsDataset {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AnalyticsLineage {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AnalyticsEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AnalyticsValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AnalyticsApproval {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AnalyticsSchedule {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AnalyticsLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AnalyticsComplianceMatrix {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AnalyticsAuditRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AnalyticsSubscription {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AnalyticsHealthIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 170: Enterprise Master Data, Reference Data & Metadata Governance Platform
model MasterDataDomain {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MasterEntity {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CanonicalRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model GoldenRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ReferenceDataset {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ReferenceValue {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseMetadataCatalog {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MetadataDefinition {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BusinessGlossary {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BusinessTerm {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DataSteward {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseDataOwner {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DataClassificationProfile {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EntityRelationshipDefinition {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseEnterpriseDataQualityRule {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MasterDataApproval {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MetadataEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MetadataValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseMetadataLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MetadataComplianceMatrix {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MetadataAuditRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MetadataPublication {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MetadataHealthIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 171: Enterprise Integration, Messaging & Workflow Connectivity Governance Platform
model EnterpriseIntegration {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model IntegrationEndpoint {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseIntegrationConnector {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model IntegrationContract {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model IntegrationMapping {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MessageChannel {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MessageQueue {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EventBus {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkflowDefinition {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkflowStep {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model IntegrationTransformation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FileTransferDefinition {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model IntegrationPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model IntegrationDeployment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model IntegrationEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model IntegrationValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model IntegrationLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model IntegrationComplianceMatrix {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model IntegrationAuditRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model IntegrationSchedule {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model IntegrationSubscription {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model IntegrationHealthIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ConnectivityRegistry {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 172: Enterprise Portfolio, Investment & Capital Allocation Governance Platform
model EnterpriseEnterprisePortfolio {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PortfolioCategory {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model InvestmentProgram {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseStrategicInitiative {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BusinessCase {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model InvestmentProposal {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model InvestmentApproval {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CapitalAllocation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FundingCommitment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FundingRelease {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BenefitRealizationPlan {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BenefitMetric {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PortfolioRoadmap {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PortfolioScenario {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model InvestmentEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model InvestmentValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model InvestmentLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model InvestmentComplianceMatrix {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model InvestmentAuditRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ExecutiveReviewCycle {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PortfolioHealthIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PortfolioGovernanceBoard {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CapitalPlanningCycle {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 173: Enterprise Performance Management, Planning & Corporate Planning Governance Platform
model EnterprisePlanningCycle {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model StrategicPlanningPeriod {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model OperationalPlanningCycle {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CorporateObjective {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model StrategicGoal {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PlanningAssumption {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PerformanceTarget {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterprisePlanningScenario {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PlanningBaseline {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ExecutivePlanningReview {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PlanningApproval {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CorporateScorecard {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PerformanceMeasure {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PlanningCalendar {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PerformanceEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PerformanceValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PerformanceLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PerformanceComplianceMatrix {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PerformanceAuditRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PlanningGovernanceBoard {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ExecutivePerformanceReview {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CorporatePerformanceIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PlanningHealthIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 174: Enterprise Service Lifecycle, Customer Support & Customer Success Governance Platform
model EnterpriseServiceCatalog {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ServiceOffering {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CustomerEntitlement {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseServiceRequest {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CustomerCase {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CaseAssignment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EscalationPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EscalationRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseCustomerSuccessPlan {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SuccessMilestone {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model OnboardingProgram {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RenewalProgram {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseServiceCommitment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseServiceEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ServiceValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseServiceLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ServiceComplianceMatrix {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ServiceAuditRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseKnowledgeReference {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SupportQueue {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CustomerFeedbackRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ServiceHealthIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ServiceGovernanceBoard {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 175: Enterprise Asset Performance, Reliability & Predictive Maintenance Governance Platform
model EnterpriseMaintenanceProgram {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MaintenanceStrategy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MaintenanceWorkOrder {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PreventiveMaintenancePlan {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CorrectiveMaintenanceRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ReliabilityEngineeringStudy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FailureModeAnalysis {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EquipmentInspection {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EquipmentConditionEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SparePartInventory {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SparePartReservation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MaintenanceShutdown {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CorporateMaintenanceSchedule {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseMaintenanceExecution {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MaintenanceEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MaintenanceValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MaintenanceLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ReliabilityIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MaintenanceComplianceMatrix {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MaintenanceAuditRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EquipmentAvailabilityRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MaintenanceGovernanceBoard {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ReliabilityReview {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 176: Enterprise Business Rules, Decision Management & Policy Automation Governance Platform
model EnterpriseBusinessRule {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BusinessRuleCategory {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DecisionTable {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DecisionService {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RuleCondition {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RuleAction {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RuleVersion {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RuleExecutionRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PolicyDefinition {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PolicyClause {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EligibilityCriteria {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ValidationRule {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CalculationRule {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ApprovalMatrix {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseDecisionEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DecisionValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseDecisionLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PolicyComplianceMatrix {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DecisionAuditRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RuleDeployment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RuleGovernanceBoard {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DecisionHealthIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PolicyExecutionHistory {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 177: Enterprise Workflow, Case Management & Human Task Governance Platform
model EnterpriseWorkflow {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkflowTemplate {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkflowVersion {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkflowInstance {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkflowCheckpoint {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkflowTransition {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model HumanTask {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TaskAssignment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TaskQueue {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ManualApproval {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkflowEscalation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseBusinessCase {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CaseMilestone {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CaseTimeline {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkflowEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkflowValidation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkflowLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkflowComplianceMatrix {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkflowAuditRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkflowGovernanceBoard {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TaskExecutionHistory {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkflowHealthIndex {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WorkflowExecutionSnapshot {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 178: Enterprise Treasury, Cash, Liquidity & Capital Markets Governance Platform
model TreasuryOrganization {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TreasuryPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BankAccount {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CashPosition {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CashPool {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LiquidityReserve {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TreasuryTransaction {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PaymentInstruction {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PaymentApproval {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BankRelationship {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ForeignExchangeExposure {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model HedgingInstrument {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DebtFacility {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LoanAgreement {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TreasuryInvestment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CapitalMarketInstrument {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LetterOfCredit {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BankGuarantee {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TreasuryComplianceRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TreasuryLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TreasuryAuditRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TreasuryRiskAssessment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TreasurySnapshot {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 179: Enterprise Tax, Statutory Reporting & Fiscal Compliance Governance Platform
model TaxJurisdiction {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TaxAuthority {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TaxRegistration {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TaxPeriod {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FiscalCalendar {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TaxReturn {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TaxFiling {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FilingSchedule {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TaxAssessment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TaxLiability {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model IndirectTax {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DirectTax {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WithholdingTax {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TransferPricingPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model StatutoryReport {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FinancialStatement {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ReportingObligation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TaxEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TaxAuditRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TaxComplianceLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FiscalControl {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RegulatorySubmission {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TaxGovernanceSnapshot {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 180: Enterprise Logistics, Fleet, Transportation & Distribution Governance Platform
model LogisticsNetwork {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseDistributionCenter {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TransportationHub {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FleetVehicle {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model VehicleAssignment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DriverCertification {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CarrierOrganization {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CarrierContract {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TransportationOrder {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FreightShipment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ShipmentManifest {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DeliveryEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RouteDefinition {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RouteCheckpoint {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FleetMaintenanceRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FuelConsumptionRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TransportationCompliance {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CustomsDeclaration {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CrossBorderShipment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FleetTelemetrySnapshot {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LogisticsLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TransportationAuditRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LogisticsGovernanceSnapshot {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 181: Enterprise Research, Development, Laboratory & Innovation Governance Platform
model ResearchProgram {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ResearchPortfolio {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ResearchProject {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ResearchProposal {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ResearchMilestone {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ResearchFunding {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LaboratoryFacility {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LaboratoryEquipment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model LaboratoryExperiment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ExperimentProtocol {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ExperimentExecution {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ExperimentEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PrototypeAsset {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PrototypeRevision {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EngineeringPrototype {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PatentPortfolio {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PatentApplication {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model IntellectualPropertyRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model InnovationSubmission {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TechnologyTransfer {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ScientificPublication {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ResearchGovernanceLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ResearchGovernanceSnapshot {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 182: Enterprise Product Safety, Regulatory Certification & Product Compliance Governance Platform
model ProductSafetyProgram {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductSafetyRequirement {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SafetyHazardRegister {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model HazardAssessment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SafetyControl {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SafetyValidationRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductCertification {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CertificationAuthority {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CertificationApplication {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RegulatoryFramework {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RegulatoryRequirement {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ConformityAssessment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ComplianceEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductLabeling {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductRestriction {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductRecall {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RecallExecution {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FieldCorrectiveAction {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PostMarketSurveillance {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RegulatoryInspection {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ComplianceDeviation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductComplianceLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductComplianceSnapshot {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 183: Enterprise Customer Experience, Omnichannel Engagement & Journey Governance Platform
model EnterpriseCustomerJourney {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model JourneyStage {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model JourneyCheckpoint {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CustomerInteraction {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model InteractionChannel {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model OmnichannelSession {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CustomerCommunication {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseCommunicationTemplate {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CustomerNotification {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CustomerPreference {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FeedbackRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SatisfactionSurvey {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ExperiencePolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ServiceTouchpoint {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model JourneyEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EngagementCampaign {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CampaignExecution {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseCustomerEscalation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DigitalExperienceMetric {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ExperienceAudit {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CustomerJourneyLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CustomerJourneySnapshot {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ExperienceGovernanceRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 184: Enterprise Digital Commerce, Marketplace & Order Governance Platform
model CommerceStore {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommerceCatalog {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductCatalogEntry {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ProductCollection {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MarketplaceIntegration {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommerceChannel {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ShoppingCart {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CartItem {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CustomerOrder {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model OrderLineItem {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model OrderFulfillment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model OrderShipment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model OrderInvoice {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommercePricingPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PromotionCampaign {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CouponDefinition {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model GiftCard {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseSubscriptionPlan {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SubscriptionContract {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DigitalPaymentRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommerceAuditRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommerceGovernanceLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CommerceGovernanceSnapshot {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 185: Enterprise Billing, Revenue Recognition & Financial Settlement Governance Platform
model BillingAccount {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BillingProfile {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BillingCycle {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BillingSchedule {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model InvoiceDocument {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model InvoiceLine {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RevenueRecognitionRule {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RevenueRecognitionEvent {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DeferredRevenueRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AccountsReceivableLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CustomerStatement {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PaymentAllocation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SettlementTransaction {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CollectionCase {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CreditMemo {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DebitMemo {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BillingAdjustment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model WriteOffRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RefundTransaction {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model FinancialSettlement {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RevenueAuditRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RevenueGovernanceLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RevenueGovernanceSnapshot {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 186: Enterprise Platform Runtime, Kernel Services & Distributed Execution Governance
model RuntimeNode {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RuntimeKernel {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RuntimeService {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PlatformCapability {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RuntimeRegistry {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RuntimeContext {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RuntimeHeartbeat {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ExecutionContext {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RuntimeDependency {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RuntimeLifecycle {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RuntimeState {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RuntimeCluster {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RuntimeTopology {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RuntimeCompatibility {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RuntimeVersion {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RuntimeHealth {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RuntimeEvent {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseRuntimeConfiguration {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RuntimeEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RuntimeLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RuntimeInitialization {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RuntimeIsolation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RuntimeExecutionRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 187: Enterprise Platform Operations, Deployment, Release & Environment Governance
model PlatformEnvironment {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DeploymentPackage {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseDeploymentExecution {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ReleaseTrain {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseReleaseVersion {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PlatformManifest {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseDeploymentApproval {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DeploymentEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PlatformUpgrade {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PlatformPatch {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseRollbackExecution {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MaintenanceWindow {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseEnvironmentConfiguration {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DeploymentPipeline {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DeploymentStage {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PlatformInstallation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PlatformLifecycle {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model OperationalReadiness {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PlatformDependency {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ReleaseCalendar {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PlatformOperation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PlatformExecutionLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PlatformAuditRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 188: Enterprise Extensibility, SDK, Marketplace & Developer Governance Platform
model DeveloperOrganization {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DeveloperWorkspace {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PlatformExtension {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ExtensionManifest {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ExtensionVersion {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ExtensionDependency {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ExtensionPermission {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ExtensionCertificate {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MarketplaceApplication {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MarketplaceListing {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MarketplacePublisher {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MarketplaceApproval {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SDKPackage {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SDKRelease {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model APIBundle {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ExtensionInstallation {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TenantExtension {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ExtensionCompatibility {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ExtensionExecution {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ExtensionEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MarketplaceTelemetry {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ExtensionLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PlatformCustomization {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 189: Enterprise Observability, Telemetry, Logging & Diagnostics Governance Platform
model TelemetryEvent {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model StructuredLog {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DiagnosticRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ExecutionTrace {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TraceSpan {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MetricsRegistry {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseMetricDefinition {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model RuntimeMetric {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model HealthCheck {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ServiceHealth {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PlatformHeartbeat {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ErrorRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ExceptionRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AlertDefinition {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model AlertEvent {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model DiagnosticSnapshot {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model PerformanceMeasurement {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model InstrumentationPoint {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TelemetryRetentionPolicy {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ObservabilityEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ObservabilityLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model MonitoringProfile {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model SystemBaseline {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}

// Phase 190: Enterprise Architecture, Capability Model & Technology Governance Platform
model EnterpriseArchitecture {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseBusinessCapability {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model CapabilityMap {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model BusinessFunction {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseApplicationPortfolio {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseApplication {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TechnologyPlatform {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TechnologyStack {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseTechnologyStandard {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseTechnologyLifecycle {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ArchitectureDomain {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseArchitecturePrinciple {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseArchitectureDecisionRecord {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseSolutionArchitecture {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseReferenceArchitecture {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model IntegrationLandscape {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ApplicationDependency {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model TechnologyDependency {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseArchitectureReview {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseArchitectureEvidence {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model ArchitectureCompliance {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseArchitectureLedger {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
model EnterpriseTechnologyRoadmap {
  id             String    @id @default(cuid())
  workspaceId    String
  createdAt      DateTime  @default(now())
}
