
export const AuditEngine = {
  getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const getAuditEngine = async (...[]) => ({});
export const calculateAuditEngine = async (...[]) => ({});
export const validateAuditEngine = async (...[]) => ({});
