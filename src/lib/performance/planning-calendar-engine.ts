
export const PlanningCalendarEngine = {
  getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const getPlanningCalendarEngine = async (...[]) => ({});
export const calculatePlanningCalendarEngine = async (...[]) => ({});
export const recordPlanningCalendarEngineEvents = async (...[]) => ({});
