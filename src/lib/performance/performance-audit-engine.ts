
export const PerformanceAuditEngine = {
  getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const getPerformanceAuditEngine = async (...[]) => ({});
export const calculatePerformanceAuditEngine = async (...[]) => ({});
export const recordPerformanceAuditEngineEvents = async (...[]) => ({});
