
export const LifecycleEngine = {
  getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const getLifecycleEngine = async (...[]) => ({});
export const calculateLifecycleEngine = async (...[]) => ({});
export const recordLifecycleEngineEvents = async (...[]) => ({});
