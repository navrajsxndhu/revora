
export const ApiEngine = {
  getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const getApiEngine = async (...[]) => ({});
export const calculateApiEngine = async (...[]) => ({});
export const recordApiEngineEvents = async (...[]) => ({});
