
export const SpecificationEngine = {
  getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const getSpecificationEngine = async (...[]) => ({});
export const calculateSpecificationEngine = async (...[]) => ({});
export const recordSpecificationEngineEvents = async (...[]) => ({});
