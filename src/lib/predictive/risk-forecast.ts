import { prisma } from "../prisma";

// Stub for calculating specific risk forecasts
export async function getRiskForecasts(workspaceId: string) {
  // Can be expanded to return global vs local risk comparisons
  return [];
}
