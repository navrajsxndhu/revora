
export const WorkflowIndex = {
  getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const getWorkflowIndex = async (...[]) => ({});
export const calculateWorkflowIndex = async (...[]) => ({});
export const recordWorkflowIndexEvents = async (...[]) => ({});
