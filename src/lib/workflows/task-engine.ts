
export const TaskEngine = {
  getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const getTaskEngine = async (...[]) => ({});
export const calculateTaskEngine = async (...[]) => ({});
export const recordTaskEngineEvents = async (...[]) => ({});
