
export const EnterpriseSecurityEngine = {
  orchestrateSecurity: async () => ({}), getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const orchestrateSecurity = async (workspaceId: string) => ({});
