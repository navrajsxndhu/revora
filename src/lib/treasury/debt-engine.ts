
export const DebtEngine = {
  getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const getDebtEngine = async (...[]) => ({});
export const calculateDebtEngine = async (...[]) => ({});
export const recordDebtEngineEvents = async (...[]) => ({});
