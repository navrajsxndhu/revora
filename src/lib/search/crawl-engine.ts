
export const CrawlEngine = {
  getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const validateCrawlEngine = async (...[]) => ({});
export const governCrawlEngine = async (...[]) => ({});
export const verifyCrawlEngine = async (...[]) => ({});
