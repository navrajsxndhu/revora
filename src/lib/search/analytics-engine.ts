
export const AnalyticsEngine = {
  getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const validateAnalyticsEngine = async (...[]) => ({});
export const governAnalyticsEngine = async (...[]) => ({});
export const verifyAnalyticsEngine = async (...[]) => ({});
