
export const InnovationEngine = {
  getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const getInnovationEngine = async (...[]) => ({});
export const calculateInnovationEngine = async (...[]) => ({});
export const recordInnovationEngineEvents = async (...[]) => ({});
