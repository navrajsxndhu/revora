
export const Ledger = {
  getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const getLedger = async (...[]) => ({});
export const calculateLedger = async (...[]) => ({});
export const recordLedgerEvents = async (...[]) => ({});
