
export const DocumentEngine = {
  getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const getDocumentEngine = async (...[]) => ({});
export const calculateDocumentEngine = async (...[]) => ({});
export const recordDocumentEngineEvents = async (...[]) => ({});
