
export const PublicationEngine = {
  getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const getPublicationEngine = async (...[]) => ({});
export const calculatePublicationEngine = async (...[]) => ({});
export const recordPublicationEngineEvents = async (...[]) => ({});
