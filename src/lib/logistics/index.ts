
export const Index = {
  getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const getIndex = async (...[]) => ({});
export const calculateIndex = async (...[]) => ({});
export const recordIndexEvents = async (...[]) => ({});
