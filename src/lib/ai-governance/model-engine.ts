
export const ModelEngine = {
  getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const validateModelEngine = async (...[]) => ({});
export const governModelEngine = async (...[]) => ({});
export const verifyModelEngine = async (...[]) => ({});
