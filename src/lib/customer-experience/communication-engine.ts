
export const CommunicationEngine = {
  getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const getCommunicationEngine = async (...[]) => ({});
export const calculateCommunicationEngine = async (...[]) => ({});
export const recordCommunicationEngineEvents = async (...[]) => ({});
