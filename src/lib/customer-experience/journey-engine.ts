
export const JourneyEngine = {
  getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const getJourneyEngine = async (...[]) => ({});
export const calculateJourneyEngine = async (...[]) => ({});
export const recordJourneyEngineEvents = async (...[]) => ({});
