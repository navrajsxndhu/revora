
export const InstallationEngine = {
  getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const getInstallationEngine = async (...[]) => ({});
export const calculateInstallationEngine = async (...[]) => ({});
export const recordInstallationEngineEvents = async (...[]) => ({});
