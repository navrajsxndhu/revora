
export const AmendmentEngine = {
  getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const getAmendmentEngine = async (...[]) => ({});
export const calculateAmendmentEngine = async (...[]) => ({});
export const recordAmendmentEngineEvents = async (...[]) => ({});
