
export const PreventiveMaintenanceEngine = {
  getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const getPreventiveMaintenanceEngine = async (...[]) => ({});
export const calculatePreventiveMaintenanceEngine = async (...[]) => ({});
export const recordPreventiveMaintenanceEngineEvents = async (...[]) => ({});
