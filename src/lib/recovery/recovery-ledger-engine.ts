
export const RecoveryLedgerEngine = {
  getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const getRecoveryLedgerEngine = async (...[]) => ({});
export const calculateRecoveryLedgerEngine = async (...[]) => ({});
export const recordRecoveryLedgerEngineEvents = async (...[]) => ({});
