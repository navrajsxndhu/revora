
export const DataLedgerEngine = {
  getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const getDataLedgerEngine = async (...[]) => ({});
export const calculateDataLedgerEngine = async (...[]) => ({});
export const recordDataLedgerEngineEvents = async (...[]) => ({});
