
export const QualityEngine = {
  getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const getQualityEngine = async (...[]) => ({});
export const calculateQualityEngine = async (...[]) => ({});
export const recordQualityEngineEvents = async (...[]) => ({});
