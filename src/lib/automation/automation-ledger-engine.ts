
export const AutomationLedgerEngine = {
  getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const getAutomationLedgerEngine = async (...[]) => ({});
export const calculateAutomationLedgerEngine = async (...[]) => ({});
export const recordAutomationLedgerEngineEvents = async (...[]) => ({});
