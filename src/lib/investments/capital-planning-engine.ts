
export const CapitalPlanningEngine = {
  getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const getCapitalPlanningEngine = async (...[]) => ({});
export const calculateCapitalPlanningEngine = async (...[]) => ({});
export const recordCapitalPlanningEngineEvents = async (...[]) => ({});
