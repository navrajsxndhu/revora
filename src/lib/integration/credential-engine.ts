
export const CredentialEngine = {
  getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const validateCredentialEngine = async (...[]) => ({});
export const governCredentialEngine = async (...[]) => ({});
export const verifyCredentialEngine = async (...[]) => ({});
