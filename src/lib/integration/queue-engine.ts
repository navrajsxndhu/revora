
export const QueueEngine = {
  getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const validateQueueEngine = async (...[]) => ({});
export const governQueueEngine = async (...[]) => ({});
export const verifyQueueEngine = async (...[]) => ({});
