
export const HealthEngine = {
  getOverview: async (workspaceId: string) => {
    return { data: "mock", workspaceId };
  }
};

export const validateHealthEngine = async (...[]) => ({});
export const governHealthEngine = async (...[]) => ({});
export const verifyHealthEngine = async (...[]) => ({});
