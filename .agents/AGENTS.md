# Verification Rule
To avoid build errors piling up in the codebase, proactively run `npm run build` or `npm run dev` to verify the codebase is working every 5 prompts or after any significant set of changes. Do not wait for the user to explicitly ask you to verify the build.
